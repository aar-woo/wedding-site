---
phase: quick
plan: 260529-os0
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/SaveTheDatePage.module.css
autonomous: false
requirements: [QUICK-HERO-CARD]
must_haves:
  truths:
    - "On desktop (>=768px) the hero renders as a large greeting-card-shaped frame, not a skinny phone-shaped column"
    - "The card sits over the sitting-engagement.png background with clear margins around it"
    - "Card content (couple names, date, countdown, footer) fits comfortably within the new proportions without overflow or cramping"
    - "Mobile (<768px) full-bleed hero behavior is unchanged"
  artifacts:
    - path: "src/pages/SaveTheDatePage.module.css"
      provides: "Updated @media (min-width: 768px) hero card sizing"
      contains: "@media (min-width: 768px)"
  key_links:
    - from: ".heroFrame @media block"
      to: "greeting-card proportions"
      via: "aspect-ratio + width/height sizing"
      pattern: "aspect-ratio"
---

<objective>
Reshape the desktop hero from a skinny iPhone-shaped column into a larger, elegant
greeting-card-shaped frame floating over the sitting-engagement.png background.

Purpose: A phone-shaped card reads like a screenshot, not an invitation. A generous
greeting-card frame makes the save-the-date feel like a keepsake physical card.
Output: Updated @media (min-width: 768px) block in SaveTheDatePage.module.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/pages/SaveTheDatePage.jsx
@src/pages/SaveTheDatePage.module.css

<interfaces>
Design tokens available in src/index.css (:root):
  --forest #0B1610, --gold #BF9B5A, --gold-light #D4B57A, --cream #EAE0CB, --muted #72685A
  --font-display (Cormorant Garamond), --font-body (Jost)
NOTE: Tokens cover colors + fonts only. There are no spacing/size tokens, so
dimensional layout values (aspect-ratio, height, border-radius, padding) are
structural CSS — they are NOT the "design values" CLAUDE.md requires to be tokenized.
Colors/fonts inside the card MUST continue to use the var() tokens above.

Current desktop hero (the part being replaced) in SaveTheDatePage.module.css:
  .heroFrame { width: auto; min-height: 0; height: min(92svh, 920px);
               aspect-ratio: 9 / 19.5; border-radius: 28px; box-shadow: ...; }

contentBlock is absolutely positioned to the bottom of .heroFrame and contains
GuestGreeting, .label, .coupleNames (72px), .divider, .date (28px), .location,
CountdownTimer, .footer. The full content stack must fit the new card height.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape desktop hero into greeting-card proportions</name>
  <files>src/pages/SaveTheDatePage.module.css</files>
  <action>
    In the existing `@media (min-width: 768px)` block, rewrite the `.heroFrame`
    rule so the card is a large greeting-card-shaped frame instead of a phone column.

    Replace the phone sizing:
      height: min(92svh, 920px);
      aspect-ratio: 9 / 19.5;
    with portrait greeting-card sizing:
      height: min(88svh, 900px);
      aspect-ratio: 5 / 7;
      max-width: min(92vw, 680px);
      width: auto;
    Keep `min-height: 0;`. This yields a generous portrait invitation card (5:7,
    the classic greeting/A-card ratio) that is clearly wider than a phone while
    still standing tall over the background.

    Keep the existing border-radius and box-shadow as-is (they already read as an
    elegant card edge), but soften the radius slightly to suit the larger frame:
    change `border-radius: 28px;` to `border-radius: 20px;`.

    Do NOT touch the `.page`, `.page::before`, `.page::after` rules — the full-bleed
    sitting-engagement.png background and its wash stay exactly as they are.
    Do NOT touch anything outside the `@media (min-width: 768px)` block — mobile
    full-bleed behavior must remain unchanged.
    Do NOT introduce any inline styles or hardcoded colors; colors stay tokenized.
  </action>
  <verify>
    <automated>cd /Users/aaronwoo/Desktop/aar-woo-repos-local/wedding-site && grep -q "aspect-ratio: 5 / 7" src/pages/SaveTheDatePage.module.css && grep -q "max-width: min(92vw, 680px)" src/pages/SaveTheDatePage.module.css && ! grep -q "9 / 19.5" src/pages/SaveTheDatePage.module.css && echo PASS</automated>
  </verify>
  <done>The @media (min-width: 768px) .heroFrame uses aspect-ratio 5/7 with a max-width cap; the 9/19.5 phone ratio is gone. Background rules untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Adjust desktop card content typography to fit the new frame</name>
  <files>src/pages/SaveTheDatePage.module.css</files>
  <action>
    Inside the SAME `@media (min-width: 768px)` block (after the `.heroFrame` rule),
    add desktop overrides so the content stack reads as balanced inside the wider,
    shorter 5:7 card. The base (mobile) rules are tuned for a narrow column, so on
    the roomier card give the type a little more presence and breathing room:

      .contentBlock { padding: 0 56px 56px; }
      .coupleNames { font-size: 84px; margin: 0 0 28px; }
      .date { font-size: 32px; }

    Use only these three overrides — do not restyle every element. All values are
    dimensional layout (font-size/padding/margin); colors and font-family stay
    inherited from the base rules (tokenized). Do not change line-height ratios or
    letter-spacing. Confirm no content overflows the card visually in Task 3.

    Keep everything else in the block unchanged.
  </action>
  <verify>
    <automated>cd /Users/aaronwoo/Desktop/aar-woo-repos-local/wedding-site && grep -q "font-size: 84px" src/pages/SaveTheDatePage.module.css && grep -q "padding: 0 56px 56px" src/pages/SaveTheDatePage.module.css && echo PASS</automated>
  </verify>
  <done>Desktop overrides for .contentBlock, .coupleNames, .date exist inside the @media block; no colors hardcoded; base mobile rules untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The desktop hero is now a large portrait greeting-card frame (5:7 ratio, capped
    at 680px wide / 900px tall) floating over the sitting-engagement.png background,
    replacing the previous skinny phone-shaped column. Card content type was nudged
    up to suit the wider proportions. Mobile is untouched.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the local URL in a desktop-width browser window (>=768px wide).
    2. Confirm the hero card looks like an elegant invitation/greeting card — clearly
       wider and less phone-like than before, with margins of background photo visible around it.
    3. Confirm all content (Rina & Aaron, Save the Date, May 30 2027, Oahu, countdown,
       footer) fits inside the card without overflowing or feeling cramped.
    4. Resize the window below 768px and confirm the mobile full-bleed hero is unchanged.
    5. If proportions feel off (too wide/too tall/too big/too small), describe the
       desired adjustment and I'll tune the aspect-ratio / max-width / height values.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the proportion/size adjustments you want</resume-signal>
</task>

</tasks>

<verification>
- `@media (min-width: 768px)` `.heroFrame` uses `aspect-ratio: 5 / 7` with a `max-width` cap; no `9 / 19.5` remains.
- Desktop content overrides present for `.contentBlock`, `.coupleNames`, `.date`.
- `.page`, `.page::before`, `.page::after` and all rules outside the media query are unchanged (mobile intact).
- No inline style values or hardcoded colors introduced; color/font tokens preserved.
</verification>

<success_criteria>
- Desktop hero renders as a large greeting-card-shaped frame, not a phone-sized column.
- Card content fits comfortably within the new proportions.
- Mobile full-bleed behavior unchanged.
- Human verification checkpoint approved.
</success_criteria>

<output>
After completion, create `.planning/quick/260529-os0-update-the-styling-of-the-hero-image-car/260529-os0-SUMMARY.md`
</output>
