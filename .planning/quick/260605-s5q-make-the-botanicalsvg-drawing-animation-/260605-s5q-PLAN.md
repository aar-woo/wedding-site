---
phase: quick-260605-s5q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/BotanicalSvg.jsx]
autonomous: true
requirements: [QUICK-260605-s5q]
must_haves:
  truths:
    - "BotanicalSvg still draws in during the page entrance cascade (no regression to the initial reveal)"
    - "After the initial draw, the stroke pathLength draw-in replays on a loop every 7 seconds"
    - "When the user prefers reduced motion, the botanical renders fully drawn and does NOT loop"
  artifacts:
    - path: "src/components/BotanicalSvg.jsx"
      provides: "Self-driving 7s redraw loop layered on top of the parent-driven entrance"
      contains: "repeat"
  key_links:
    - from: "src/pages/SaveTheDatePage.jsx decorations cascade"
      to: "BotanicalSvg branchVariants/dotVariants"
      via: "parent variants propagation must still trigger first draw"
      pattern: "variants=\\{branchVariants\\}"
---

<objective>
Make the BotanicalSvg stroke "draw-in" animation replay on a loop every 7 seconds,
instead of playing only once during the page-load entrance cascade.

Purpose: Add a subtle, recurring ambient life to the botanical decorations so the
keepsake scene continues to feel animated after the initial reveal settles.
Output: An updated BotanicalSvg.jsx that keeps its existing variant-driven first
draw (sequenced by SaveTheDatePage's decorations group) AND then redraws every 7s,
while fully respecting reduced-motion (no loop, statically drawn).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

<interfaces>
<!-- BotanicalSvg is rendered by SaveTheDatePage inside a parent-driven cascade. -->
<!-- The parent flips hidden→visible; BotanicalSvg's variants currently inherit -->
<!-- "visible" from that parent. Any change MUST preserve that first draw. -->

From src/pages/SaveTheDatePage.jsx (lines 89-102):
```jsx
<motion.div className={styles.decorations} variants={decorationsVariants}>
  <CornerBrackets />
  <motion.div className={styles.botanicalPair} variants={passthroughVariants}>
    <BotanicalSvg />
    <BotanicalSvg flipped />
  </motion.div>
</motion.div>
```
Note: the page sets `initial="hidden" animate="visible"` only on the root
`heroFrame`. Variants propagate DOWN. BotanicalSvg's `<motion.svg variants={svgVariants}>`
and child `<motion.path variants={branchVariants}>` inherit the parent's animate state
— they do NOT set their own `initial`/`animate`. This is why the draw runs once on load.

From src/components/BotanicalSvg.jsx (current variant shape — DO NOT regress):
```js
const EASE = [0.22, 0.61, 0.36, 1];
// branchVariants.visible: { pathLength: 1, opacity: 1, transition: { duration: 1.0, ease: EASE } }
// dotVariants.visible:    { scale: 1,     opacity: 1, transition: { duration: 0.4, ease: EASE } }
// reduceMotion => all variants resolve to the fully-drawn state with no motion
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a 7s draw-in loop to BotanicalSvg via repeat + repeatDelay</name>
  <files>src/components/BotanicalSvg.jsx</files>
  <action>
    Make the stroke draw-in (pathLength 0→1) and dot pop (scale 0→1) REPLAY every 7
    seconds, while preserving the existing parent-cascade-driven first draw and the
    reduced-motion static state. Implement entirely within BotanicalSvg.jsx — do NOT
    edit SaveTheDatePage.jsx, and do NOT add hardcoded per-element delays (timing must
    stay derived from staggerChildren + the shared transition config).

    Approach (keep variants + staggerChildren; respect CLAUDE.md animation rules):

    1. Keep the existing `svgVariants` staggerChildren so leaves still cascade branch-by-branch.

    2. Convert the draw to a self-repeating keyframe loop on the child variants. For the
       non-reduced-motion `visible` state of `branchVariants`, animate pathLength as a
       keyframe array that draws in, holds, then resets to redraw:
         - pathLength: [0, 1, 1, 0]  (draw in → hold drawn → hold drawn → snap back to undrawn)
         - opacity:    [0, 1, 1, 0]  (so the snap-back to undrawn isn't visible — fades out before resetting)
       Transition:
         - duration: 7   (one full 7-second cycle, satisfies the "every 7s" requirement)
         - times:    [0, 0.14, 0.85, 1]   (≈1.0s draw-in matching current 1.0s feel, long hold drawn, brief fade/reset tail)
         - ease:     EASE   (the existing [0.22, 0.61, 0.36, 1])
         - repeat:   Infinity
       Rationale to put in a brief code comment: pathLength can't tween 1→0 invisibly via
       repeatType, so we use a keyframe array with an opacity fade on the reset tail to make
       each cycle re-draw cleanly. This keeps the gold strokes from appearing to "erase."

    3. Apply the same keyframe-loop treatment to `dotVariants.visible`:
         - scale:   [0, 1, 1, 0]
         - opacity: [0, 1, 1, 0]
         - transition: { duration: 7, times: [0, 0.06, 0.85, 1], ease: EASE, repeat: Infinity }
       (Dots pop a bit faster than strokes draw, matching the current 0.4s vs 1.0s feel.)

    4. Reduced-motion path (reduceMotion === true): leave the existing behavior EXACTLY as-is
       — variants resolve to the fully-drawn static state ({ pathLength: 1, opacity: 1 } /
       { scale: 1, opacity: 1 }) with NO repeat, NO keyframes. The loop must only exist on
       the non-reduced-motion branch.

    5. Do NOT add `initial`/`animate` props to BotanicalSvg's motion elements — the first
       draw must keep coming from the parent decorations cascade so the page-load act
       ordering (Act 2) is unchanged. Because the child transition now has repeat:Infinity,
       once the parent flips to "visible" the child will draw and then loop on its own 7s cycle.

    Keep `staggerChildren: 0.12` on `svgVariants.visible` so each branch's 7s loop is offset
    slightly, preserving the organic branch-by-branch feel on every redraw.
  </action>
  <verify>
    <automated>cd /Users/aaronwoo/Desktop/aar-woo-repos-local/wedding-site && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - npm run build succeeds with no errors.
    - branchVariants.visible (non-reduced-motion) uses a keyframe pathLength array with
      transition { duration: 7, repeat: Infinity, ease: EASE } and a times array.
    - dotVariants.visible (non-reduced-motion) similarly loops every 7s.
    - reduceMotion branch remains static (no repeat, no keyframes).
    - No edits to SaveTheDatePage.jsx; no hardcoded per-element delay props added.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    BotanicalSvg now redraws its gold stroke botanicals on a 7-second loop after the
    initial page-entrance draw, with reduced-motion users seeing a static fully-drawn
    botanical (no loop).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the local URL.
    2. Watch the two botanical branches flanking "Rina & Aaron" draw in during page load (unchanged).
    3. Wait ~7s after settle: confirm the strokes re-draw from base to tip (leaves cascade,
       dots pop), then repeat again ~7s later — smooth, not abrupt, no visible "erasing."
    4. Confirm the redraw does not disturb the rest of the layout/text and feels ambient, not distracting.
    5. Enable "Reduce motion" (macOS: System Settings → Accessibility → Display → Reduce motion),
       hard-refresh: botanicals should appear fully drawn immediately and NOT loop.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks off (timing, erase flicker, too distracting)</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes.
- Manual: botanical strokes redraw on a ~7s cadence after initial load.
- Manual: reduced-motion shows static, fully-drawn botanicals with no looping.
- Initial page-entrance cascade (Act 2 decorations timing) is visually unchanged.
</verification>

<success_criteria>
- BotanicalSvg draw-in animation replays every 7 seconds (non-reduced-motion).
- Initial page-load draw still sequenced by SaveTheDatePage's decorations cascade.
- Reduced-motion: static, no loop.
- Only src/components/BotanicalSvg.jsx changed; CLAUDE.md animation rules respected
  (variants + staggerChildren, EASE [0.22,0.61,0.36,1], no hardcoded per-element delays).
</success_criteria>

<output>
After completion, create `.planning/quick/260605-s5q-make-the-botanicalsvg-drawing-animation-/260605-s5q-SUMMARY.md`
</output>
