---
phase: 08-frontend-hook-api-endpoint
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/guest/[id].js
  - vercel.json
  - scripts/test-guest-endpoint.js
autonomous: true
requirements: [BACK-02, BACK-03]
user_setup: []
must_haves:
  truths:
    - "GET /api/guest/:id returns HTTP 200 {id, displayName} for a known, non-deleted guest"
    - "GET /api/guest/:id returns HTTP 404 {error:'not found'} for an unknown OR soft-deleted id"
    - "GET /api/guest/:id returns HTTP 405 for non-GET methods"
    - "vercel.json routes /api/* to functions BEFORE the SPA catch-all"
    - "No VITE_-prefixed secret exists in .env* or src/; the endpoint reads process.env only"
  artifacts:
    - path: "api/guest/[id].js"
      provides: "Vercel Node serverless GET endpoint: id-only Neon lookup, 200/404/405/400"
      contains: "process.env.DATABASE_URL"
    - path: "vercel.json"
      provides: "rewrites: /api/(.*) passthrough FIRST, then /(.*) -> /index.html SPA catch-all"
      contains: "/api/(.*)"
    - path: "scripts/test-guest-endpoint.js"
      provides: "Local Node harness proving 200/404/405 against live Neon without Vercel CLI"
      contains: "import handler from '../api/guest/[id].js'"
  key_links:
    - from: "api/guest/[id].js"
      to: "process.env.DATABASE_URL"
      via: "neon(process.env.DATABASE_URL) — Node runtime only, never VITE_, never import.meta.env"
      pattern: "process\\.env\\.DATABASE_URL"
    - from: "api/guest/[id].js"
      to: "guests table (Neon)"
      via: "SELECT id, display_name WHERE id = ${id} AND deleted_at IS NULL"
      pattern: "deleted_at IS NULL"
    - from: "vercel.json"
      to: "Vercel rewrite engine"
      via: "/api/(.*) rule positioned before /(.*) catch-all"
      pattern: "/api/\\(\\.\\*\\)"
---

<objective>
Create the `GET /api/guest/:id` Vercel Node serverless function — the id-only read contract a future RSVP write path reuses — wire `vercel.json` so `/api/*` resolves ahead of the SPA catch-all, add a local test harness proving 200/404/405 against live Neon, and confirm env discipline (no `VITE_` secrets).

Purpose: Establish the durable backend lookup contract (BACK-02) and prove the secret-handling boundary holds (BACK-03). The endpoint is deliberately OFF the page-load path (D-02) — it exists only as the RSVP-foundation read contract.
Output: `api/guest/[id].js`, `vercel.json`, `scripts/test-guest-endpoint.js`, and a clean VITE_ audit. [BACK-02, BACK-03]
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/08-frontend-hook-api-endpoint/08-CONTEXT.md
@docs/identity-token-contract.md

<interfaces>
<!-- Neon query pattern + schema columns the endpoint targets. Mirror scripts/migrate.js exactly. -->

@neondatabase/serverless usage (from scripts/migrate.js — mirror this form):
```js
import { neon } from '@neondatabase/serverless';
const sql = neon(process.env.DATABASE_URL);
const rows = await sql`SELECT ... WHERE col = ${value}`;  // tagged-template is SQL-injection safe
```

guests table schema (from scripts/migrate.js — columns the endpoint reads/filters):
```
id           TEXT PRIMARY KEY   -- 21-char nanoid; the credential
display_name TEXT NOT NULL      -- source of truth for displayName (D-07)
deleted_at   TIMESTAMPTZ        -- soft-delete; filter WHERE deleted_at IS NULL (D-08)
```

Handler signature (research Pattern 1 — DECISIVE recommendation: legacy Node helper for req.query.id):
```js
export default async function handler(req, res) {
  // req.method, req.query.id available; res.status(code).json(body) chainable
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create api/guest/[id].js Node serverless endpoint</name>
  <files>api/guest/[id].js</files>
  <read_first>
    - scripts/migrate.js (the neon() form + guests schema columns: id, display_name, deleted_at)
    - scripts/generate-links.js (the @neondatabase/serverless neon() tagged-template usage to mirror)
    - docs/identity-token-contract.md §4 (HMAC verified server-side only — NOT on this read path)
    - .planning/phases/08-frontend-hook-api-endpoint/08-CONTEXT.md (D-06, D-07, D-08, D-09)
    - 08-RESEARCH.md Pattern 1 (complete handler skeleton — legacy export default handler(req,res))
  </read_first>
  <action>
    Create api/guest/[id].js as a Vercel Node serverless function (Node runtime, NOT Edge — deprecated).
    Use the legacy handler signature (research Pattern 1 DECISIVE recommendation — gives req.query.id directly):
    `export default async function handler(req, res) { ... }`. The file is ESM ("type":"module").

    Import only `import { neon } from '@neondatabase/serverless';`. Do NOT import anything from scripts/
    (especially NOT scripts/lib/token.js — D-06: no token verify on this read path; trust-boundary).

    Logic, in order:
      1. Method guard: `if (req.method !== 'GET') return res.status(405).json({ error: 'method not allowed' });`
      2. `const { id } = req.query;` then guard: `if (!id || typeof id !== 'string') return res.status(400).json({ error: 'bad request' });`
      3. `const sql = neon(process.env.DATABASE_URL);` (read DATABASE_URL via process.env — D-09; NEVER import.meta.env, NEVER VITE_).
      4. Log id ONLY, never the name (PITFALLS — PII in logs, D-06): `console.log(\`GET /api/guest/${id}\`);`
      5. Query (D-06, D-07, D-08): parameterized tagged-template
         ```js
         const rows = await sql`
           SELECT id, display_name
           FROM guests
           WHERE id = ${id}
             AND deleted_at IS NULL
         `;
         ```
      6. `if (rows.length === 0) return res.status(404).json({ error: 'not found' });` (unknown OR soft-deleted both 404 — D-08).
      7. On hit: `const { id: guestId, display_name } = rows[0]; return res.status(200).json({ id: guestId, displayName: display_name });` (displayName from DB column — D-07).
      8. Wrap the DB call in try/catch; on error log without PII (`console.error(\`DB error id=${id}:\`, err.message)`) and `return res.status(500).json({ error: 'internal server error' });`.

    Do NOT read or verify the ?t= token on this path (D-06). Do NOT update first_seen_at (research Open Q #2 — keep read-only). No first_seen_at write.
  </action>
  <verify>
    <automated>node --check 'api/guest/[id].js'</automated>
    <automated>grep -q "process.env.DATABASE_URL" 'api/guest/[id].js' && grep -q "deleted_at IS NULL" 'api/guest/[id].js'</automated>
    <automated>grep -q "405" 'api/guest/[id].js' && grep -q "404" 'api/guest/[id].js' && grep -q "displayName" 'api/guest/[id].js'</automated>
    <automated>! grep -q "import.meta.env" 'api/guest/[id].js' && ! grep -q "scripts/lib" 'api/guest/[id].js' && ! grep -q "VITE_" 'api/guest/[id].js'</automated>
  </verify>
  <acceptance_criteria>
    - `node --check 'api/guest/[id].js'` exits 0 (valid ESM syntax)
    - `grep -q "process.env.DATABASE_URL"` matches (Node env read — D-09)
    - `grep -q "deleted_at IS NULL"` matches (soft-delete filter — D-08)
    - `grep -q "405"`, `grep -q "404"`, `grep -q "displayName"` all match (method guard + not-found + response shape)
    - `! grep -q "import.meta.env"` (no Vite client env)
    - `! grep -q "scripts/lib"` (trust boundary — no token lib on read path)
    - `! grep -q "VITE_"` (no client-prefixed secret)
  </acceptance_criteria>
  <done>Endpoint: GET-only (405 else), 400 on missing id, id-only Neon SELECT filtered on deleted_at, 200 {id,displayName} / 404 {error:'not found'}, reads process.env.DATABASE_URL, logs id never name, no token verify, no scripts/ import.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create vercel.json (api passthrough first, SPA catch-all second)</name>
  <files>vercel.json</files>
  <read_first>
    - .planning/phases/08-frontend-hook-api-endpoint/08-CONTEXT.md (D-10 — ordering is critical)
    - 08-RESEARCH.md Pattern 3 (exact vercel.json contents + ordering rationale, PITFALLS #5)
  </read_first>
  <action>
    Create vercel.json at the repo root with exactly two rewrites IN THIS ORDER (D-10):
    ```json
    {
      "$schema": "https://openapi.vercel.sh/vercel.json",
      "rewrites": [
        { "source": "/api/(.*)", "destination": "/api/$1" },
        { "source": "/(.*)",     "destination": "/index.html" }
      ]
    }
    ```
    The `/api/(.*)` passthrough MUST be the FIRST element in the rewrites array — Vercel evaluates
    rewrites sequentially, so a catch-all first would swallow /api/* and the function would never fire
    (PITFALLS #5 / anti-pattern #4). Do NOT add a `functions` runtime block — the default current Node
    LTS is correct (research: omit for a cleaner config). Valid JSON only.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8'))"</automated>
    <automated>node -e "const j=require('./vercel.json'); const r=j.rewrites; if(!r[0].source.startsWith('/api')) { console.error('api rule not first'); process.exit(1);} if(r[1].destination!=='/index.html'){console.error('catch-all not index.html');process.exit(1);} console.log('OK: api-first, spa catch-all second');"</automated>
  </verify>
  <acceptance_criteria>
    - vercel.json is valid JSON (`JSON.parse` succeeds)
    - `rewrites[0].source` starts with `/api` (passthrough is FIRST — D-10)
    - `rewrites[1].destination` is `/index.html` (SPA catch-all is SECOND)
    - The node assertion script prints "OK: api-first, spa catch-all second" and exits 0
  </acceptance_criteria>
  <done>vercel.json exists with /api/(.*) passthrough as rewrites[0] and /(.*) -> /index.html as rewrites[1]; valid JSON; no functions block.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add endpoint test harness, run live 200/404/405, audit VITE_ secrets</name>
  <files>scripts/test-guest-endpoint.js</files>
  <read_first>
    - api/guest/[id].js (the handler under test — created in Task 1)
    - 08-RESEARCH.md §"Local Testing Strategy" (the fake req/res harness pattern)
    - 08-VALIDATION.md (Wave 0 harness + Manual-Only endpoint check)
    - docs/identity-token-contract.md §5 (env naming — the VITE_ audit rule)
  </read_first>
  <action>
    Create scripts/test-guest-endpoint.js — a Node harness that imports the handler and exercises it
    with a fake req/res (no Vercel CLI needed). Mirror research's Local Testing Strategy:
    ```js
    import handler from '../api/guest/[id].js';
    const id = process.argv[2] || 'test-id';
    const method = process.argv[3] || 'GET';
    const req = { method, query: { id } };
    const res = {
      _status: 200,
      status(code) { this._status = code; return this; },
      json(body) { console.log(`HTTP ${this._status}:`, JSON.stringify(body, null, 2)); return this; },
    };
    await handler(req, res);
    ```
    Run it three ways against the LIVE Neon DB (DATABASE_URL is already in .env.local from Phase 7):
      - A real id from links.csv (or `node --env-file=.env.local scripts/generate-links.js scripts/guests.example.csv` first to mint one, then read an id from links.csv) -> expect HTTP 200 {id, displayName}.
      - A fake id (e.g. `fake-id-000`) -> expect HTTP 404 {error:"not found"}.
      - A real-or-fake id with method POST (3rd arg) -> expect HTTP 405.
    Confirm each prints the expected status. The harness is a dev tool; committing it is fine (documented).

    Then run the BACK-03 env audit: `grep -rn "VITE_" .env* src/` must return zero SECRET matches
    (the only allowed hit is the explanatory comment in .env.example; no GUEST_TOKEN_SECRET / DATABASE_URL).
    Also `grep -rn "import.meta.env" src/` must show no secret reads.
  </action>
  <verify>
    <automated>node --check scripts/test-guest-endpoint.js</automated>
    <automated>grep -q "import handler from '../api/guest/\[id\].js'" scripts/test-guest-endpoint.js</automated>
    <automated>node --env-file=.env.local scripts/test-guest-endpoint.js fake-id-000 2>&1 | grep -q "HTTP 404"</automated>
    <automated>node --env-file=.env.local scripts/test-guest-endpoint.js fake-id-000 POST 2>&1 | grep -q "HTTP 405"</automated>
    <automated>test -z "$(grep -rn 'VITE_' .env* src/ | grep -iE 'GUEST_TOKEN_SECRET|DATABASE_URL')" && echo "no VITE_ secret leak"</automated>
    <automated>test -z "$(grep -rn 'import.meta.env' src/ | grep -iE 'SECRET|DATABASE')" && echo "no secret import.meta.env"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/test-guest-endpoint.js` exits 0
    - Harness imports the handler from `'../api/guest/[id].js'`
    - `node --env-file=.env.local scripts/test-guest-endpoint.js fake-id-000` prints "HTTP 404" (unknown id -> 404, D-08)
    - `node --env-file=.env.local scripts/test-guest-endpoint.js fake-id-000 POST` prints "HTTP 405" (non-GET guard)
    - A real id (from links.csv) prints "HTTP 200" with {id, displayName} (executor mints one if links.csv is empty — see action)
    - `grep -rn "VITE_" .env* src/` has NO match containing GUEST_TOKEN_SECRET or DATABASE_URL (BACK-03)
    - `grep -rn "import.meta.env" src/` has no secret reads (BACK-03)
  </acceptance_criteria>
  <done>Harness proves live 200 (real id) / 404 (fake id) / 405 (POST) against Neon; VITE_ audit returns zero secret matches; no import.meta.env secret reads.</done>
</task>

</tasks>

<verification>
- `node --check 'api/guest/[id].js'`, `node --check scripts/test-guest-endpoint.js`, valid `vercel.json` JSON
- vercel.json: `/api/(.*)` rewrite is rewrites[0]; `/(.*) -> /index.html` is rewrites[1]
- Live harness: real id -> HTTP 200 {id,displayName}; fake id -> HTTP 404 {error:"not found"}; POST -> HTTP 405
- `grep -rn "VITE_" .env* src/` -> zero secret matches (BACK-03); endpoint reads process.env, no import.meta.env, no scripts/ import
- Criterion #4 (cold deep-link /i/:id -> SPA not 404) is NOT locally verifiable — verify in Phase 9 after Vercel deploy (see VALIDATION.md Manual-Only)
</verification>

<success_criteria>
- GET /api/guest/:id returns 200 {id, displayName} for a known guest, 404 for unknown/soft-deleted, 405 for non-GET (BACK-02)
- displayName sourced from the Neon display_name column; lookup filters deleted_at IS NULL (D-07, D-08)
- vercel.json routes /api/* before the SPA catch-all (BACK-02, criterion #4 wiring)
- Signing secret and DB URL are server-only (process.env in api/), never VITE_-prefixed, never in src/ (BACK-03)
- Endpoint does not import scripts/lib/token.js and performs no token verification (D-06 trust boundary)
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-hook-api-endpoint/08-02-SUMMARY.md`
</output>
