---
phase: 08-frontend-hook-api-endpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useGuestName.js
  - src/App.jsx
autonomous: true
requirements: [BACK-02]
must_haves:
  truths:
    - "Opening /?t=<valid-token> shows the decoded guest name instantly with no network call"
    - "Opening /?to=Test+Name shows 'Test Name' (legacy dev/preview shortcut still works)"
    - "Opening / (no token, no ?to=) shows 'Our Beloved Guests' with no error"
    - "Navigating to /i/<id> renders the same SaveTheDatePage as /"
    - "document.title becomes 'Save the Date – For {name}' only when a real name resolves"
  artifacts:
    - path: "src/hooks/useGuestName.js"
      provides: "Token-first guest name resolution (t -> to -> fallback), keeps {name,hasName} shape"
      contains: "decodeGuestToken"
    - path: "src/App.jsx"
      provides: "Routes block: /i/:id and /* both render SaveTheDatePage inside MotionConfig"
      contains: "path=\"/i/:id\""
  key_links:
    - from: "src/hooks/useGuestName.js"
      to: "src/lib/decodeGuestToken.js"
      via: "import decodeGuestToken (browser-safe), NOT scripts/lib/token.js"
      pattern: "from '\\.\\./lib/decodeGuestToken\\.js'"
    - from: "src/App.jsx"
      to: "react-router"
      via: "import { Routes, Route } from 'react-router'"
      pattern: "from 'react-router'"
---

<objective>
Wire the live SPA to resolve guest identity entirely from the URL token — instant, client-side, zero network round-trip — and add the durable `/i/:id` route.

Purpose: The personalized greeting must appear instantly the moment a guest opens their link (the single load-bearing UX rule). The name rides in the signed `?t=` token and is decoded client-side via the already-built `decodeGuestToken`. The `/i/:id` path establishes the durable identity segment a future RSVP flow reuses.
Output: Modified `src/hooks/useGuestName.js` (token-first resolution) and `src/App.jsx` (Routes block). [BACK-02]
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/08-frontend-hook-api-endpoint/08-CONTEXT.md
@docs/identity-token-contract.md

<interfaces>
<!-- Contracts the executor needs — use directly, no codebase exploration required. -->

From src/lib/decodeGuestToken.js (browser-safe, secret-free — the ONLY token util the hook may import):
```js
// Returns { id, name } on success, null on ANY failure (never throws).
// Validates both id and name are non-empty strings. iat is dropped.
export function decodeGuestToken(token /* the ?t= value */) : { id: string, name: string } | null
```

Current src/hooks/useGuestName.js (the rewrite target — extend in place, keep return shape):
```js
function useGuestName() {
  const [searchParams] = useSearchParams();   // from 'react-router'
  // currently reads ONLY ?to=
  return { name, hasName };                    // GuestGreeting.jsx consumes ONLY .name
}
```

src/components/GuestGreeting.jsx (DO NOT MODIFY — consumes only .name):
```jsx
const { name } = useGuestName();
return <p>For <span>{name}</span></p>;
```

Current src/App.jsx (the modify target):
```jsx
import { MotionConfig } from 'framer-motion';
import SaveTheDatePage from './pages/SaveTheDatePage';
function App() {
  return (<MotionConfig reducedMotion="user"><SaveTheDatePage /></MotionConfig>);
}
```

src/main.jsx (DO NOT MODIFY — BrowserRouter already mounted here):
```jsx
import { BrowserRouter } from 'react-router';
// <BrowserRouter><App /></BrowserRouter>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite useGuestName for token-first resolution</name>
  <files>src/hooks/useGuestName.js</files>
  <read_first>
    - src/hooks/useGuestName.js (current ?to= reader — extend in place, keep {name,hasName})
    - src/lib/decodeGuestToken.js (the browser-safe util to import; returns {id,name}|null, never throws)
    - docs/identity-token-contract.md §6 (Fallback Contract — null maps to "Our Beloved Guests")
    - .planning/phases/08-frontend-hook-api-endpoint/08-CONTEXT.md (D-01, D-02, D-03, D-04)
  </read_first>
  <action>
    Rewrite src/hooks/useGuestName.js extending it IN PLACE (D-01). Add a second import:
    `import { decodeGuestToken } from '../lib/decodeGuestToken.js';` — this exact relative path,
    the browser-safe util. NEVER import scripts/lib/token.js (Pitfall 3: would pull node:crypto into
    the browser bundle).

    Resolution order (D-01), implemented as plain synchronous logic in the hook body:
      1. Read `const t = searchParams.get('t');` — if `t` is present, call `decodeGuestToken(t)`.
         Guard the null return (Pitfall 5): `const decoded = decodeGuestToken(t); if (decoded && decoded.name) { name = decoded.name; hasName = true; }`.
      2. If still !hasName, fall back to the legacy `?to=` dev/preview shortcut (D-04):
         `const raw = searchParams.get('to'); const trimmed = raw ? raw.trim() : ''; if (trimmed.length > 0) { name = trimmed; hasName = true; }`.
      3. If still !hasName, leave the fallback `name = 'Our Beloved Guests'` (D-03 wording).

    Initialize `let name = 'Our Beloved Guests'; let hasName = false;` before the checks.

    Keep the document.title side-effect in a useEffect, but set it ONLY when a real name resolves (D-03):
    `useEffect(() => { if (hasName) { document.title = \`Save the Date – For ${name}\`; } }, [hasName, name]);`
    When !hasName, leave the default title untouched (do NOT set it).

    Keep the return shape exactly `return { name, hasName };` so GuestGreeting.jsx (consumes only .name)
    stays unchanged. Do NOT call fetch or any API — greeting is 100% client-side decode (D-02).
    Do NOT use useParams — the greeting only needs ?t= (research Pattern 4; path :id is server-concern only).
    Keep `import { useSearchParams } from 'react-router';` (NOT react-router-dom).
  </action>
  <verify>
    <automated>node --check src/hooks/useGuestName.js</automated>
    <automated>grep -q "decodeGuestToken" src/hooks/useGuestName.js && grep -q "from '../lib/decodeGuestToken.js'" src/hooks/useGuestName.js</automated>
    <automated>grep -q "searchParams.get('t')" src/hooks/useGuestName.js && grep -q "searchParams.get('to')" src/hooks/useGuestName.js && grep -q "Our Beloved Guests" src/hooks/useGuestName.js</automated>
    <automated>! grep -q "scripts/lib" src/hooks/useGuestName.js && ! grep -q "import.meta.env" src/hooks/useGuestName.js && ! grep -q "fetch(" src/hooks/useGuestName.js</automated>
    <automated>node --test src/lib/decodeGuestToken.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/hooks/useGuestName.js` exits 0 (valid syntax)
    - `grep -q "decodeGuestToken" src/hooks/useGuestName.js` matches AND import path is `'../lib/decodeGuestToken.js'`
    - `grep -q "searchParams.get('t')"` and `searchParams.get('to')` both match (both resolution steps present)
    - `grep -q "Our Beloved Guests"` matches (fallback wording preserved)
    - `! grep -q "scripts/lib" src/hooks/useGuestName.js` (no Node-only token lib imported)
    - `! grep -q "import.meta.env" src/hooks/useGuestName.js` and `! grep -q "fetch(" src/hooks/useGuestName.js` (no secrets, no network — D-02)
    - `node --test src/lib/decodeGuestToken.test.js` still green (decode layer the hook reuses)
  </acceptance_criteria>
  <done>Hook resolves token -> ?to= -> "Our Beloved Guests"; returns {name,hasName}; sets document.title only when named; no fetch, no scripts/lib import, no import.meta.env.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Routes block to App.jsx (/i/:id and catch-all)</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (current: renders SaveTheDatePage directly inside MotionConfig)
    - src/main.jsx (BrowserRouter already mounted — do NOT touch)
    - .planning/phases/08-frontend-hook-api-endpoint/08-CONTEXT.md (D-05)
    - 08-RESEARCH.md Pattern 4 (exact App.jsx change)
  </read_first>
  <action>
    Modify src/App.jsx to wrap SaveTheDatePage in a `<Routes>` block (D-05). Add the import
    `import { Routes, Route } from 'react-router';` (from 'react-router', NOT react-router-dom).
    Keep `MotionConfig reducedMotion="user"` as the OUTERMOST wrapper so all page animations still
    inherit the reduced-motion config. Inside MotionConfig, render:
    ```jsx
    <Routes>
      <Route path="/i/:id" element={<SaveTheDatePage />} />
      <Route path="/*" element={<SaveTheDatePage />} />
    </Routes>
    ```
    Both routes render the SAME SaveTheDatePage — the greeting differs only by what useGuestName
    resolves from ?t= / ?to=. Do NOT add useParams or pass :id as a prop (the greeting doesn't need it;
    :id is the durable identity segment for the future RSVP route). Do NOT modify main.jsx.
  </action>
  <verify>
    <automated>node --check src/App.jsx</automated>
    <automated>grep -q "from 'react-router'" src/App.jsx && grep -q "Routes" src/App.jsx && grep -q "Route" src/App.jsx</automated>
    <automated>grep -q 'path="/i/:id"' src/App.jsx && grep -q "MotionConfig" src/App.jsx</automated>
    <automated>! grep -q "react-router-dom" src/App.jsx</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/App.jsx` exits 0
    - `grep -q "from 'react-router'" src/App.jsx` matches (NOT react-router-dom)
    - `grep -q 'path="/i/:id"' src/App.jsx` matches (durable identity route defined)
    - `grep -q "MotionConfig" src/App.jsx` matches (MotionConfig still wraps Routes)
    - `! grep -q "react-router-dom" src/App.jsx` (project convention)
    - `npm run build` succeeds (Vite bundles the routed app cleanly)
  </acceptance_criteria>
  <done>App.jsx renders Routes (/i/:id + /*) inside MotionConfig; both routes render SaveTheDatePage; imports from 'react-router'; build passes.</done>
</task>

</tasks>

<verification>
- `node --check src/hooks/useGuestName.js` and `node --check src/App.jsx` both pass
- `npm run build` succeeds (the routed SPA bundles)
- `node --test src/lib/decodeGuestToken.test.js` green (decode layer intact)
- `grep -rn "VITE_" src/` shows no secret reads; `grep -rn "import.meta.env" src/` shows no secret access
- Manual browser smoke (phase gate, see VALIDATION.md Manual-Only): `/?t=<real-token>` -> decoded name; `/?to=Test+Name` -> "Test Name"; `/` -> "Our Beloved Guests"; `/i/<any-id>` loads the page; DevTools Network shows NO /api/guest request on load (D-02)
</verification>

<success_criteria>
- useGuestName resolves token-first, then ?to=, then "Our Beloved Guests" — all client-side, no fetch
- App.jsx defines /i/:id and /* routes inside MotionConfig, imported from 'react-router'
- GuestGreeting.jsx unchanged and still renders the resolved name
- No VITE_ secret, no import.meta.env, no scripts/lib import in any modified src/ file
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-hook-api-endpoint/08-01-SUMMARY.md`
</output>
