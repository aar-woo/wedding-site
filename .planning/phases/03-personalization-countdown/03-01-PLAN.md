---
phase: 03-personalization-countdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useGuestName.js
  - src/components/GuestGreeting.jsx
  - src/components/GuestGreeting.module.css
  - src/pages/SaveTheDatePage.jsx
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03, PERS-04]
must_haves:
  truths:
    - "Opening /?to=The+Johnson+Family shows 'For The Johnson Family' above the Save the Date label"
    - "Opening /?to=Mike+%26+Sarah shows 'For Mike & Sarah' (URL-encoded ampersand decoded)"
    - "Opening the site with no ?to= shows 'For Our Beloved Guests'"
    - "When ?to= is present, the browser tab title reads 'Save the Date – For {name}'"
    - "When ?to= is absent, the tab title is left at its default (not the fallback name)"
  artifacts:
    - path: "src/hooks/useGuestName.js"
      provides: "Hook reading ?to= via useSearchParams, trim, fallback, document.title side-effect"
      contains: "useSearchParams"
    - path: "src/components/GuestGreeting.jsx"
      provides: "Renders 'For' + guest name, consumes useGuestName"
      contains: "useGuestName"
    - path: "src/components/GuestGreeting.module.css"
      provides: "Italic Cormorant Garamond greeting styles using design tokens"
      contains: "var(--font-display)"
  key_links:
    - from: "src/components/GuestGreeting.jsx"
      to: "src/hooks/useGuestName.js"
      via: "import + call useGuestName()"
      pattern: "useGuestName"
    - from: "src/pages/SaveTheDatePage.jsx"
      to: "src/components/GuestGreeting.jsx"
      via: "rendered as first child of content block, above .label"
      pattern: "GuestGreeting"
    - from: "src/hooks/useGuestName.js"
      to: "document.title"
      via: "useEffect setting title when name present"
      pattern: "document.title"
---

<objective>
Add guest personalization to the save-the-date page: read the `?to=` URL param, render a "For [Guest Name]" greeting at the TOP of the existing content block (above the "Save the Date" label), with a graceful fallback and a personalized browser tab title.

Purpose: Each guest opens their own link and immediately sees their name — the warmth of a personally addressed invitation (PERS-01..04).
Output: `useGuestName` hook, `GuestGreeting` component + CSS module, and the greeting wired into `SaveTheDatePage`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-personalization-countdown/03-CONTEXT.md
@CLAUDE.md
@src/index.css
@src/pages/SaveTheDatePage.jsx
@src/pages/SaveTheDatePage.module.css

<interfaces>
<!-- Design tokens already defined in src/index.css — use via var(): -->
<!--   --forest #0B1610, --gold #BF9B5A, --gold-light #D4B57A, --cream #EAE0CB, --muted #72685A -->
<!--   --font-display 'Cormorant Garamond' (italic 300–700 loaded), --font-body 'Jost' -->

<!-- BrowserRouter is already mounted in src/main.jsx, importing from 'react-router' (v7 unified package). -->
<!-- useSearchParams must likewise be imported `from 'react-router'` (NOT 'react-router-dom'). -->

<!-- Existing content block JSX (src/pages/SaveTheDatePage.jsx) — greeting inserts as the FIRST child, above .label:
  <div className={styles.contentBlock}>
    <p className={styles.label}>Save the Date</p>
    <h1 className={styles.coupleNames}>Rina &amp; Aaron</h1>
    ...
-->

<!-- useSearchParams signature: const [searchParams] = useSearchParams(); searchParams.get('to') -->
<!-- searchParams.get() returns the ALREADY URL-DECODED value (Mike+%26+Sarah -> "Mike & Sarah"), or null if absent. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create useGuestName hook</name>
  <files>src/hooks/useGuestName.js</files>
  <read_first>
    - src/hooks/ does not exist yet — create the directory
    - .planning/phases/03-personalization-countdown/03-CONTEXT.md (decisions D-01..D-04)
    - CLAUDE.md (Guest personalization section + URL pattern)
    - src/main.jsx (confirms `from 'react-router'` import convention for v7)
  </read_first>
  <action>
    Create `src/hooks/useGuestName.js` exporting a default (or named) hook `useGuestName()`.

    Behavior (copy these decisions exactly — D-01..D-04):
    - Import `{ useSearchParams }` `from 'react-router'` (NOT 'react-router-dom' — matches main.jsx; D-01).
    - Import `{ useEffect }` from 'react'.
    - Inside the hook: `const [searchParams] = useSearchParams();`
    - Read `const raw = searchParams.get('to');` — `.get()` returns the already-URL-decoded value or `null`.
    - Compute `const trimmed = raw ? raw.trim() : '';` (D-04: trim surrounding whitespace).
    - Treat empty/whitespace-only as absent (D-02): `const hasName = trimmed.length > 0;`
    - `const name = hasName ? trimmed : 'Our Beloved Guests';` — fallback string MUST be exactly "Our Beloved Guests".
    - document.title side-effect (D-03): in a `useEffect` keyed on `[hasName, trimmed]`:
        - if `hasName`, set ``document.title = `Save the Date – For ${trimmed}`;``
          (use the EN DASH "–" exactly, with single spaces: "Save the Date – For {name}").
        - else do NOT modify document.title (leave the default from index.html). Do not append the fallback name to the title.
    - Return `{ name, hasName }` (component only needs `name`; `hasName` is available if useful).

    Constraints: no design values here (logic only); no UI libraries.
  </action>
  <verify>
    <automated>grep -q "from 'react-router'" src/hooks/useGuestName.js && grep -q "useSearchParams" src/hooks/useGuestName.js && grep -q "Our Beloved Guests" src/hooks/useGuestName.js && grep -q "Save the Date – For" src/hooks/useGuestName.js && grep -q "document.title" src/hooks/useGuestName.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useGuestName.js` exists and contains `useSearchParams` imported `from 'react-router'`
    - File contains the exact fallback string `Our Beloved Guests`
    - File sets `document.title` to a template literal containing `Save the Date – For ` (EN DASH, trailing space before name)
    - `searchParams.get('to')` is used to read the param (relies on built-in URL decoding for PERS-04)
    - The decoded value is `.trim()`-ed and empty/whitespace falls back (PERS-02)
  </acceptance_criteria>
  <done>Hook reads ?to=, trims, falls back to "Our Beloved Guests", and sets document.title only when a name is present. PERS-01, PERS-02, PERS-03, PERS-04 logic complete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create GuestGreeting component + CSS module</name>
  <files>src/components/GuestGreeting.jsx, src/components/GuestGreeting.module.css</files>
  <read_first>
    - src/hooks/useGuestName.js (created in Task 1 — its return shape { name, hasName })
    - .planning/phases/03-personalization-countdown/03-CONTEXT.md (decisions D-05, D-07)
    - src/index.css (token names: --font-display, --cream, --gold-light)
    - src/pages/SaveTheDatePage.module.css (existing type scale — match label/location size rhythm)
    - CLAUDE.md (GuestGreeting.jsx contract; "no inline styles for design values — always CSS Modules")
  </read_first>
  <action>
    Create `src/components/` directory.

    `src/components/GuestGreeting.module.css`:
    - `.greeting` class for the single greeting line:
        - `font-family: var(--font-display);`
        - `font-style: italic;` (D-05: italic Cormorant Garamond — italic weights already loaded)
        - `font-weight: 400;`
        - a modest size — set `font-size: 22px;` (line reads as a soft personal opener, below the 72px names; tune within elegance)
        - `line-height: 1.3;`
        - `letter-spacing: 0.01em;`
        - `color: var(--cream);` (D-05: "For" in cream)
        - `margin: 0 0 16px;` (sits above the label; gives breathing room before "Save the Date")
    - `.name` class for the guest name span:
        - `color: var(--gold-light);` (D-05: name in gold-light)
        - inherits the italic display font from `.greeting`
    All values via `var(--*)` tokens — NO raw hex, NO inline style attributes for design values.

    `src/components/GuestGreeting.jsx`:
    - Import `styles from './GuestGreeting.module.css'`.
    - Import `useGuestName` from '../hooks/useGuestName.js'.
    - `const { name } = useGuestName();`
    - Render a single line: `<p className={styles.greeting}>For <span className={styles.name}>{name}</span></p>`
      (the literal word "For" in cream, the name in gold-light — D-04/D-05).
    - Export default `GuestGreeting`.

    Do NOT add any Framer Motion / entrance animation here (resting state only — that choreography is Phase 4).
  </action>
  <verify>
    <automated>grep -q "useGuestName" src/components/GuestGreeting.jsx && grep -q "var(--font-display)" src/components/GuestGreeting.module.css && grep -q "font-style: italic" src/components/GuestGreeting.module.css && grep -q "var(--gold-light)" src/components/GuestGreeting.module.css && grep -q ">For" src/components/GuestGreeting.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/GuestGreeting.jsx` imports and calls `useGuestName` and renders the literal word `For` plus `{name}`
    - `src/components/GuestGreeting.module.css` `.greeting` uses `var(--font-display)` and `font-style: italic`
    - The name span color is `var(--gold-light)`; the line/"For" color is `var(--cream)`
    - No raw hex values and no inline `style={{...}}` design values in either file
    - No Framer Motion import in this component (resting state only)
  </acceptance_criteria>
  <done>GuestGreeting renders "For [name]" in italic Cormorant Garamond, name in gold-light, consuming useGuestName. No animation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire GuestGreeting into the page above the label, build</name>
  <files>src/pages/SaveTheDatePage.jsx</files>
  <read_first>
    - src/pages/SaveTheDatePage.jsx (current content-block JSX order)
    - src/components/GuestGreeting.jsx (created Task 2)
    - .planning/phases/03-personalization-countdown/03-CONTEXT.md (D-06 placement: greeting at TOP, above the label)
  </read_first>
  <action>
    Edit `src/pages/SaveTheDatePage.jsx`:
    - Add `import GuestGreeting from '../components/GuestGreeting.jsx';` at the top.
    - Insert `<GuestGreeting />` as the FIRST child inside `<div className={styles.contentBlock}>`, immediately ABOVE `<p className={styles.label}>Save the Date</p>` (D-06).
    - Resulting content-block order: GuestGreeting -> label -> coupleNames -> divider -> date -> location -> footer.
    - Do NOT touch the hero, scrim, or existing copy. Do NOT add animation wrappers.

    Then run `npm run build` and confirm it exits 0.
  </action>
  <verify>
    <automated>grep -q "import GuestGreeting" src/pages/SaveTheDatePage.jsx && node -e "const s=require('fs').readFileSync('src/pages/SaveTheDatePage.jsx','utf8');const g=s.indexOf('GuestGreeting');const l=s.indexOf('Save the Date');process.exit(g>-1 && g<l ? 0 : 1)" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/SaveTheDatePage.jsx` imports `GuestGreeting` and renders `<GuestGreeting />` as the first child of `.contentBlock`
    - `<GuestGreeting />` appears BEFORE the `Save the Date` label in the JSX source (assert ordering — D-06)
    - `npm run build` exits 0
    - Hero, scrim, and existing copy elements are unchanged
  </acceptance_criteria>
  <done>Greeting renders at the top of the content block above the label; build passes. Full personalization flow (URL -> greeting -> title) is live.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0.
- Manual smoke (executor may use `npm run dev`): `/?to=The+Johnson+Family` shows "For The Johnson Family"; `/?to=Mike+%26+Sarah` shows "For Mike & Sarah"; no `?to=` shows "For Our Beloved Guests"; tab title becomes "Save the Date – For {name}" only when `?to=` is present.
- grep: `useSearchParams` imported `from 'react-router'`; no inline design values / raw hex in greeting files.
</verification>

<success_criteria>
- PERS-01: `?to=` read via `useSearchParams`, shown as "For [name]" — met.
- PERS-02: absent/empty `?to=` falls back to "Our Beloved Guests" — met.
- PERS-03: `document.title` set to "Save the Date – For {name}" when present — met.
- PERS-04: URL-encoded names decode correctly (handled by `searchParams.get`) — met.
- Greeting placed above the "Save the Date" label (D-06); no animation added (deferred to Phase 4).
</success_criteria>

<output>
After completion, create `.planning/phases/03-personalization-countdown/03-01-SUMMARY.md`
</output>
