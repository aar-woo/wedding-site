---
phase: 03-personalization-countdown
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/CountdownTimer.jsx
  - src/components/CountdownTimer.module.css
  - src/pages/SaveTheDatePage.jsx
autonomous: true
requirements: [CNT-01, CNT-02]
must_haves:
  truths:
    - "A live days / hours / minutes / seconds countdown to May 30, 2027 is visible in a horizontal row"
    - "The countdown ticks once per second — the seconds value visibly changes every second"
    - "Each unit's number animates individually when its value changes (old number exits, new enters)"
    - "Each number has a small uppercase unit label (DAYS / HRS / MIN / SEC) beneath/beside it"
    - "When the target date is reached or passed, the countdown freezes at 00 / 00 / 00 / 00 (no negatives)"
    - "The countdown sits below the location line and above the footer note"
  artifacts:
    - path: "src/components/CountdownTimer.jsx"
      provides: "Live countdown to May 30 2027 with per-tick AnimatePresence digit swap"
      contains: "AnimatePresence"
    - path: "src/components/CountdownTimer.module.css"
      provides: "Jost numerals in cream, muted uppercase unit labels, horizontal row layout"
      contains: "var(--font-body)"
  key_links:
    - from: "src/components/CountdownTimer.jsx"
      to: "the target Date May 30 2027"
      via: "new Date(2027, 4, 30) (month is 0-indexed -> 4 = May)"
      pattern: "2027"
    - from: "src/components/CountdownTimer.jsx"
      to: "framer-motion AnimatePresence"
      via: "motion.span keyed on the numeric value"
      pattern: "key=\\{"
    - from: "src/pages/SaveTheDatePage.jsx"
      to: "src/components/CountdownTimer.jsx"
      via: "rendered between .location and .footer in the content block"
      pattern: "CountdownTimer"
---

<objective>
Add a live countdown to May 30, 2027 (days / hours / minutes / seconds in a row) below the location line and above the footer note. Each unit's number animates individually when it ticks, using Framer Motion AnimatePresence with the number as the React key — the only motion in Phase 3.

Purpose: A quiet, ticking countdown adds anticipation without shouting over the names and date (CNT-01, CNT-02).
Output: `CountdownTimer` component + CSS module, wired into `SaveTheDatePage` between location and footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-personalization-countdown/03-CONTEXT.md
@CLAUDE.md
@src/index.css
@src/pages/SaveTheDatePage.jsx
@src/pages/SaveTheDatePage.module.css

<interfaces>
<!-- Design tokens (src/index.css), use via var():
       --cream #EAE0CB (numerals), --muted #72685A (unit labels),
       --font-body 'Jost', --font-display 'Cormorant Garamond' -->

<!-- framer-motion ^12 is installed. Import: import { motion, AnimatePresence } from 'framer-motion'; -->

<!-- Easing (CLAUDE.md): [0.22, 0.61, 0.36, 1] (ease out quart). -->

<!-- Target date: May 30, 2027. JS Date months are 0-indexed -> new Date(2027, 4, 30, 0, 0, 0). -->

<!-- Content block in src/pages/SaveTheDatePage.jsx — countdown inserts between .location and .footer:
     <p className={styles.location}>Oahu, Hawaii</p>
     {/* <CountdownTimer /> goes HERE */}
     <p className={styles.footer}>Formal invitation to follow</p>
-->

<!-- After Plan 03-01, the content block already starts with <GuestGreeting /> above the label.
     Do not remove or reorder it. Only insert the countdown between location and footer. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create CountdownTimer component + CSS module</name>
  <files>src/components/CountdownTimer.jsx, src/components/CountdownTimer.module.css</files>
  <read_first>
    - .planning/phases/03-personalization-countdown/03-CONTEXT.md (decisions D-08..D-12)
    - CLAUDE.md (CountdownTimer.jsx contract; easing [0.22, 0.61, 0.36, 1]; "no inline design values")
    - src/index.css (tokens: --font-body, --cream, --muted)
    - src/pages/SaveTheDatePage.module.css (existing type scale — keep numerals in the location/footer size range, smaller than names/date)
  </read_first>
  <action>
    Create `src/components/CountdownTimer.jsx` and `src/components/CountdownTimer.module.css`.

    Logic (D-08, D-12):
    - Import `{ useState, useEffect }` from 'react' and `{ motion, AnimatePresence }` from 'framer-motion'.
    - Define `const TARGET = new Date(2027, 4, 30, 0, 0, 0);` (month 4 = May; D-08). May leave time at local midnight.
    - A helper `getTimeLeft()` computing `const diff = TARGET.getTime() - Date.now();`:
        - if `diff <= 0`, return `{ days: 0, hours: 0, minutes: 0, seconds: 0 }` (D-12 freeze at zeros — never negative).
        - else compute: days = floor(diff / 86400000); hours = floor(diff / 3600000) % 24; minutes = floor(diff / 60000) % 60; seconds = floor(diff / 1000) % 60.
    - State: `const [t, setT] = useState(getTimeLeft);`
    - `useEffect`: `const id = setInterval(() => setT(getTimeLeft()), 1000); return () => clearInterval(id);` (ticks every second — D-08). Empty dep array.

    Render (D-10 style, D-11 animation):
    - A container `<div className={styles.countdown}>` laying out four units in a horizontal row (flex).
    - Map over an array of `{ value, label }`: `[{value:t.days,label:'DAYS'},{value:t.hours,label:'HRS'},{value:t.minutes,label:'MIN'},{value:t.seconds,label:'SEC'}]`.
    - For each unit render `<div className={styles.unit}>`:
        - The animated number — wrap in `<AnimatePresence mode="popLayout" initial={false}>` and render
          `<motion.span key={value} className={styles.number} initial={{opacity:0, y:-6}} animate={{opacity:1, y:0}} exit={{opacity:0, y:6}} transition={{duration:0.35, ease:[0.22,0.61,0.36,1]}}>{String(value).padStart(2,'0')}</motion.span>`.
          The `key={value}` (the numeric value) drives the per-tick exit/enter swap (CNT-02 / D-11). Pad to 2 digits.
        - The unit label: `<span className={styles.label}>{label}</span>` (uppercase already in the string; CSS adds tracking).
    - Export default `CountdownTimer`.
    - Wrap each number in a fixed-size positioned cell if needed so the popLayout swap doesn't cause layout jump (e.g. `.number` is `position: relative` inside a min-width `.numberCell`); keep it simple.

    CSS (`CountdownTimer.module.css`) — all values via `var(--*)`, NO raw hex, NO inline design values:
    - `.countdown`: `display: flex; justify-content: center; gap: 20px; margin: 0 0 32px;` (sits above footer; mirror the location's 32px bottom rhythm).
    - `.unit`: `display: flex; flex-direction: column; align-items: center; min-width: 44px;`
    - `.number`: `font-family: var(--font-body); color: var(--cream); font-size: 22px; font-weight: 400; line-height: 1; font-variant-numeric: tabular-nums;` (modest — location/footer range, distinctly smaller than names/date; D-10).
    - `.numberCell` (if used): `position: relative; height: 22px;` to anchor AnimatePresence swaps.
    - `.label`: `font-family: var(--font-body); color: var(--muted); font-size: 9px; font-weight: 400; text-transform: uppercase; letter-spacing: 0.18em; margin-top: 6px;` (small muted uppercase tracked unit label; D-10).

    Do NOT add any entrance/staggerChildren choreography or other motion — the ONLY motion is the per-tick digit swap (D-11). Do not add Ken Burns/parallax/botanical anything.
  </action>
  <verify>
    <automated>grep -q "AnimatePresence" src/components/CountdownTimer.jsx && grep -Eq "key=\{value\}|key=\{t\." src/components/CountdownTimer.jsx && grep -q "2027" src/components/CountdownTimer.jsx && grep -q "setInterval" src/components/CountdownTimer.jsx && grep -q "0.22, 0.61, 0.36, 1" src/components/CountdownTimer.jsx && grep -q "var(--font-body)" src/components/CountdownTimer.module.css && grep -q "var(--muted)" src/components/CountdownTimer.module.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/CountdownTimer.jsx` imports `AnimatePresence` and `motion` from `framer-motion`
    - The animated number `motion.span` uses `key={value}` (or equivalent bound to the numeric value) so each tick swaps (CNT-02)
    - The target date references `2027` and uses `new Date(2027, 4, 30, ...)` (May, 0-indexed month)
    - `setInterval(..., 1000)` drives a once-per-second tick and is cleared on unmount
    - Freeze logic returns all zeros when `diff <= 0` (no negative numbers — D-12)
    - Transition uses easing `[0.22, 0.61, 0.36, 1]`
    - `.number` uses `var(--font-body)` + `var(--cream)`; `.label` uses `var(--muted)` + `text-transform: uppercase`
    - No raw hex / inline design values; no entrance choreography or non-countdown motion
  </acceptance_criteria>
  <done>CountdownTimer renders four units (DAYS/HRS/MIN/SEC) to May 30 2027, ticking each second, with per-tick AnimatePresence digit swaps and a freeze-at-zero floor. CNT-01 + CNT-02 logic complete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire CountdownTimer between location and footer, build</name>
  <files>src/pages/SaveTheDatePage.jsx</files>
  <read_first>
    - src/pages/SaveTheDatePage.jsx (current content block, now including <GuestGreeting /> from Plan 03-01)
    - src/components/CountdownTimer.jsx (created in Task 1)
    - .planning/phases/03-personalization-countdown/03-CONTEXT.md (D-09 placement: below location, above footer)
  </read_first>
  <action>
    Edit `src/pages/SaveTheDatePage.jsx`:
    - Add `import CountdownTimer from '../components/CountdownTimer.jsx';` at the top (alongside the existing GuestGreeting import).
    - Insert `<CountdownTimer />` in the content block BETWEEN `<p className={styles.location}>Oahu, Hawaii</p>` and `<p className={styles.footer}>Formal invitation to follow</p>` (D-09).
    - Resulting final content-block order: GuestGreeting -> label -> coupleNames -> divider -> date -> location -> CountdownTimer -> footer.
    - Do NOT remove or reorder GuestGreeting (from Plan 03-01). Do NOT touch hero/scrim/copy. Do NOT add animation wrappers around existing elements.

    Then run `npm run build` and confirm it exits 0.
  </action>
  <verify>
    <automated>grep -q "import CountdownTimer" src/pages/SaveTheDatePage.jsx && node -e "const s=require('fs').readFileSync('src/pages/SaveTheDatePage.jsx','utf8');const loc=s.indexOf('Oahu');const cd=s.indexOf('CountdownTimer />');const ft=s.indexOf('Formal invitation');process.exit(loc>-1 && cd>loc && ft>cd ? 0 : 1)" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/SaveTheDatePage.jsx` imports `CountdownTimer` and renders `<CountdownTimer />`
    - In the JSX source, `<CountdownTimer />` appears AFTER the `Oahu, Hawaii` location line and BEFORE the `Formal invitation to follow` footer (D-09 ordering)
    - `<GuestGreeting />` from Plan 03-01 is still present above the label (not removed)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Countdown renders below the location and above the footer; build passes. Final content order matches D-06 + D-09. CNT-01 + CNT-02 live on the page.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0.
- Manual smoke (`npm run dev`): a four-unit DAYS/HRS/MIN/SEC row is visible between "Oahu, Hawaii" and the footer; the SEC value changes every second; the changing digit visibly fades/slides on each tick.
- grep: `AnimatePresence` + `key={value}` present; target date references `2027`; numerals use `var(--font-body)`/`var(--cream)`, labels use `var(--muted)`; easing `[0.22, 0.61, 0.36, 1]`.
</verification>

<success_criteria>
- CNT-01: live days/hours/minutes/seconds countdown to May 30, 2027 in a row — met.
- CNT-02: each number animates individually on tick via AnimatePresence key swap — met.
- Placement matches D-09 (below location, above footer); freeze-at-zero per D-12; only per-tick motion (no Phase 4 choreography).
</success_criteria>

<output>
After completion, create `.planning/phases/03-personalization-countdown/03-02-SUMMARY.md`
</output>
