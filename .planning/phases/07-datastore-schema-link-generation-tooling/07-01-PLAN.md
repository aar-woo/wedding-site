---
phase: 07-datastore-schema-link-generation-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - scripts/migrate.js
  - scripts/guests.example.csv
  - .env.example
autonomous: true
requirements: [BACK-01, LINK-04]
must_haves:
  truths:
    - "A guests table can be created in Neon with all locked columns by running one committed script"
    - "Re-running the migration on an existing table does not error (idempotent)"
    - "guests.csv and links.csv can never be accidentally committed"
    - "A non-developer can see the CSV format and required env vars without real data"
  artifacts:
    - path: "scripts/migrate.js"
      provides: "Idempotent Neon DDL for the guests table"
      contains: "CREATE TABLE IF NOT EXISTS guests"
    - path: ".gitignore"
      provides: "Git-safety for private guest data"
      contains: "guests.csv"
    - path: "scripts/guests.example.csv"
      provides: "Committed fake-data CSV documenting the input format"
      contains: "display_name,email"
    - path: ".env.example"
      provides: "Documentation of required env vars (no real secrets)"
      contains: "DATABASE_URL"
    - path: "package.json"
      provides: "nanoid + @neondatabase/serverless deps, csv-parse devDep, db:* script shorthands"
      contains: "@neondatabase/serverless"
  key_links:
    - from: "scripts/migrate.js"
      to: "process.env.DATABASE_URL"
      via: "neon(process.env.DATABASE_URL)"
      pattern: "neon\\(process\\.env\\.DATABASE_URL\\)"
---

<objective>
Stand up the project's datastore foundation: install the Phase 7 dependencies, lock down git-safety for the private guest list, ship a committed idempotent Neon migration that creates the `guests` table, and document the input CSV format + required env vars.

Purpose: Without this, no DB script can run and the private guest list (real names + emails) could leak into git history. This plan makes the table createable and the repo safe BEFORE any real data exists.

Output: Updated `package.json` (deps + script shorthands), `.gitignore` (guests.csv / links.csv), `scripts/migrate.js`, `scripts/guests.example.csv`, `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-datastore-schema-link-generation-tooling/07-CONTEXT.md
@.planning/phases/07-datastore-schema-link-generation-tooling/07-RESEARCH.md

<interfaces>
<!-- The migration uses the @neondatabase/serverless HTTP tagged-template driver. -->
<!-- Locked schema columns (07-CONTEXT.md D-06 + ROADMAP Phase 7 success criterion #1): -->

guests table columns (exact):
  id                TEXT PRIMARY KEY        -- nanoid ~21 chars (minted in JS, never serial)
  display_name      TEXT NOT NULL
  email             TEXT UNIQUE             -- nullable; stable upsert key (07 D-04/D-06)
  created_at        TIMESTAMPTZ NOT NULL DEFAULT now()
  first_seen_at     TIMESTAMPTZ            -- nullable; written by Phase 8, NOT this phase
  deleted_at        TIMESTAMPTZ            -- nullable; soft-delete marker (07 D-05, deleted_at chosen over is_active)
  rsvp_status       TEXT                  -- nullable RSVP stub
  rsvp_count        INTEGER               -- nullable RSVP stub
  rsvp_submitted_at TIMESTAMPTZ            -- nullable RSVP stub

Driver import (07-RESEARCH Pattern 1):
  import { neon } from '@neondatabase/serverless';
  const sql = neon(process.env.DATABASE_URL);
  await sql`CREATE TABLE IF NOT EXISTS guests ( ... )`;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies, package.json script shorthands, and .env.example</name>
  <read_first>
    - package.json (the file being modified — current deps + scripts)
    - .planning/phases/07-datastore-schema-link-generation-tooling/07-RESEARCH.md (§Standard Stack: exact versions + dependency vs devDependency rationale; §Pattern 9: --env-file shorthands)
    - docs/identity-token-contract.md (§5: env var naming — GUEST_TOKEN_SECRET, DATABASE_URL, never VITE_-prefixed)
  </read_first>
  <action>
    1. Install the Phase 7 dependencies (run from repo root):
       `npm install @neondatabase/serverless@^1.1.0 nanoid@^5.1.0`
       `npm install -D csv-parse@^6.2.0`
       This adds `@neondatabase/serverless` and `nanoid` to `dependencies` (both reused by the Phase 8 api/ function), and `csv-parse` to `devDependencies` (local-only).

    2. In `package.json`, add these two script shorthands to the `"scripts"` object (keep existing dev/build/lint/preview):
       "db:migrate": "node --env-file=.env.local scripts/migrate.js",
       "db:generate-links": "node --env-file=.env.local scripts/generate-links.js"
       Rationale: Node 20+ `--env-file` loads `.env.local` with zero extra deps (07 D-08, RESEARCH Pattern 9). Node v23 is installed.

    3. Create `.env.example` at the repo root with EXACTLY these documented vars (no real values — placeholders only):
       # Copy to .env.local (gitignored) and fill in real values.
       # NEVER prefix these with VITE_ — they are server-only secrets (see docs/identity-token-contract.md §5).
       GUEST_TOKEN_SECRET=replace-with-a-long-random-secret
       DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require
       # Optional. The deployed site origin used to build links. If unset, generate-links.js emits a clearly-marked placeholder host.
       SITE_BASE_URL=https://your-domain.example
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['@neondatabase/serverless']) throw new Error('missing neon dep'); if(!p.dependencies.nanoid) throw new Error('missing nanoid dep'); if(!p.devDependencies['csv-parse']) throw new Error('missing csv-parse devDep'); if(!p.scripts['db:migrate']||!p.scripts['db:generate-links']) throw new Error('missing db scripts'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains `@neondatabase/serverless` and `nanoid`
    - `package.json` devDependencies contains `csv-parse`
    - `package.json` scripts contains `db:migrate` with value `node --env-file=.env.local scripts/migrate.js`
    - `package.json` scripts contains `db:generate-links` with value `node --env-file=.env.local scripts/generate-links.js`
    - `.env.example` exists and contains the strings `GUEST_TOKEN_SECRET`, `DATABASE_URL`, and `SITE_BASE_URL`
    - `grep -E "VITE_(GUEST_TOKEN_SECRET|DATABASE_URL)" .env.example` produces NO output
    - `node_modules/@neondatabase/serverless` and `node_modules/nanoid` and `node_modules/csv-parse` directories exist
  </acceptance_criteria>
  <done>npm install succeeds; package.json has the three new deps + two db: scripts; .env.example documents all three env vars with no VITE_ prefix.</done>
</task>

<task type="auto">
  <name>Task 2: Add git-safety entries and the committed example CSV</name>
  <read_first>
    - .gitignore (the file being modified — currently has *.local but NOT guests.csv / links.csv)
    - .planning/phases/07-datastore-schema-link-generation-tooling/07-CONTEXT.md (D-09: gitignore covers guests.csv, links.csv; commit guests.example.csv)
    - .planning/phases/07-datastore-schema-link-generation-tooling/07-RESEARCH.md (§Pitfall 6: add to gitignore BEFORE creating either file; §Code Examples: guests.example.csv content)
  </read_first>
  <action>
    1. Append to `.gitignore` (after the existing `*.local` line) a clearly-labeled section:
       # Private guest data — NEVER commit (real names + emails + personalized links)
       guests.csv
       links.csv
       Note: `.env.local` is already covered by the existing `*.local` pattern — do NOT add a redundant entry.

    2. Create `scripts/guests.example.csv` (COMMITTED fake data — this is the template a non-dev copies to `guests.csv`). Use exactly the header `display_name,email` and fake rows that exercise quoting + unicode + ampersand:
       display_name,email
       The Johnson Family,johnsons@example.com
       "Mike & Sarah",mike.sarah@example.com
       The García Family,garcia@example.com
       "Dr. & Mrs. Smith",smiths@example.com
       (Wrap any value containing a comma or ampersand-with-comma in double quotes; `Mike & Sarah` is quoted to document that quoting is supported. `García` documents UTF-8.)
  </action>
  <verify>
    <automated>git check-ignore guests.csv links.csv</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains a line that is exactly `guests.csv`
    - `.gitignore` contains a line that is exactly `links.csv`
    - `git check-ignore guests.csv links.csv` outputs both `guests.csv` and `links.csv`
    - `git check-ignore .env.local` outputs `.env.local` (still covered by existing `*.local`)
    - `scripts/guests.example.csv` exists and its first line is exactly `display_name,email`
    - `git check-ignore scripts/guests.example.csv` produces NO output (the example IS committed)
  </acceptance_criteria>
  <done>`.gitignore` blocks guests.csv + links.csv; `git check-ignore` confirms both; scripts/guests.example.csv exists with the correct header and is NOT ignored.</done>
</task>

<task type="auto">
  <name>Task 3: Write the idempotent Neon migration script</name>
  <read_first>
    - .planning/phases/07-datastore-schema-link-generation-tooling/07-RESEARCH.md (§Pattern 1 neon() HTTP driver; §Pattern 2 idempotent migration incl. WHY ADD COLUMN IF NOT EXISTS follows CREATE TABLE; §Code Examples: Complete Schema DDL)
    - .planning/phases/07-datastore-schema-link-generation-tooling/07-CONTEXT.md (D-06 schema columns; D-08 idempotent migration via @neondatabase/serverless HTTP driver)
    - scripts/check-token-url.js (existing script style: ESM imports, top-level await, console output, process.exit on failure)
  </read_first>
  <action>
    Create `scripts/migrate.js` — an idempotent DDL script run via `node --env-file=.env.local scripts/migrate.js` (or `npm run db:migrate`). Requirements (concrete):

    1. ESM, top-level await (project is `"type":"module"`). First line comment documents the run command.
    2. Import: `import { neon } from '@neondatabase/serverless';`
    3. Fail fast with a human-readable error (and `process.exit(1)`) if `process.env.DATABASE_URL` is unset:
       if (!process.env.DATABASE_URL) { console.error('DATABASE_URL is not set. Provision Neon and add DATABASE_URL to .env.local, then run: npm run db:migrate'); process.exit(1); }
    4. `const sql = neon(process.env.DATABASE_URL);`
    5. Run the CREATE TABLE with the EXACT locked schema:
       await sql`
         CREATE TABLE IF NOT EXISTS guests (
           id                TEXT PRIMARY KEY,
           display_name      TEXT NOT NULL,
           email             TEXT UNIQUE,
           created_at        TIMESTAMPTZ NOT NULL DEFAULT now(),
           first_seen_at     TIMESTAMPTZ,
           deleted_at        TIMESTAMPTZ,
           rsvp_status       TEXT,
           rsvp_count        INTEGER,
           rsvp_submitted_at TIMESTAMPTZ
         )
       `;
    6. THEN run additive idempotent ALTERs (so a table created by an older migration version gains any missing columns — RESEARCH Pattern 2 explains why this is required after CREATE TABLE IF NOT EXISTS):
       await sql`ALTER TABLE guests ADD COLUMN IF NOT EXISTS email TEXT UNIQUE`;
       await sql`ALTER TABLE guests ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ`;
       await sql`ALTER TABLE guests ADD COLUMN IF NOT EXISTS first_seen_at TIMESTAMPTZ`;
       await sql`ALTER TABLE guests ADD COLUMN IF NOT EXISTS rsvp_status TEXT`;
       await sql`ALTER TABLE guests ADD COLUMN IF NOT EXISTS rsvp_count INTEGER`;
       await sql`ALTER TABLE guests ADD COLUMN IF NOT EXISTS rsvp_submitted_at TIMESTAMPTZ`;
    7. On success: `console.log('Migration complete: guests table ready.');`
    8. Do NOT use Pool. Do NOT use a migration framework. Do NOT populate first_seen_at (that's Phase 8).

    NOTE: This script CANNOT run end-to-end without a provisioned Neon DB + DATABASE_URL. That live run is gated by the user-action checkpoint in plan 07-03. This task only AUTHORS the script; verification here is static (parse + structure), not a live DB call.
  </action>
  <verify>
    <automated>node --check scripts/migrate.js && grep -q "CREATE TABLE IF NOT EXISTS guests" scripts/migrate.js && grep -q "email TEXT UNIQUE" scripts/migrate.js && grep -q "deleted_at TIMESTAMPTZ" scripts/migrate.js && grep -q "ADD COLUMN IF NOT EXISTS" scripts/migrate.js && grep -q "neon(process.env.DATABASE_URL)" scripts/migrate.js && echo "STRUCTURE OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/migrate.js` exits 0 (valid ESM syntax)
    - `scripts/migrate.js` contains `CREATE TABLE IF NOT EXISTS guests`
    - `scripts/migrate.js` contains `email TEXT UNIQUE`, `deleted_at TIMESTAMPTZ`, `first_seen_at TIMESTAMPTZ`, `rsvp_status TEXT`, `rsvp_count INTEGER`, `rsvp_submitted_at TIMESTAMPTZ`
    - `scripts/migrate.js` contains `id                TEXT PRIMARY KEY` (or `id TEXT PRIMARY KEY`)
    - `scripts/migrate.js` contains `ADD COLUMN IF NOT EXISTS` (at least the additive ALTERs)
    - `scripts/migrate.js` contains `neon(process.env.DATABASE_URL)`
    - `scripts/migrate.js` contains a fail-fast guard referencing `DATABASE_URL` and `process.exit(1)`
    - `scripts/migrate.js` does NOT contain `first_seen_at = now()` or any write to first_seen_at in an INSERT/UPDATE
    - `grep -i "Pool" scripts/migrate.js` produces NO output (HTTP driver only)
  </acceptance_criteria>
  <done>scripts/migrate.js parses, uses the neon() HTTP driver, creates the guests table with all 9 locked columns, runs additive idempotent ALTERs, fails fast on missing DATABASE_URL, and never touches first_seen_at.</done>
</task>

</tasks>

<verification>
- `node -e` package.json check passes (Task 1)
- `git check-ignore guests.csv links.csv` lists both (Task 2)
- `node --check scripts/migrate.js` + grep structure checks pass (Task 3)
- No real secrets or guest data committed: `.env.example` has placeholders only; `guests.example.csv` is fake data
</verification>

<success_criteria>
- Dependencies installed (`@neondatabase/serverless`, `nanoid`, `csv-parse`) and `db:*` scripts present
- `.gitignore` blocks `guests.csv` and `links.csv`; example CSV committed
- `scripts/migrate.js` exists, parses, and contains the exact locked DDL for all 9 columns (BACK-01)
- `.env.example` documents `GUEST_TOKEN_SECRET`, `DATABASE_URL`, `SITE_BASE_URL` with no VITE_ prefix (LINK-04 git-safety + BACK-03 convention)
</success_criteria>

<output>
After completion, create `.planning/phases/07-datastore-schema-link-generation-tooling/07-01-SUMMARY.md`
</output>
