---
phase: 07-datastore-schema-link-generation-tooling
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - scripts/generate-links.js
  - scripts/check-link.js
autonomous: false
requirements: [BACK-01, LINK-04]
user_setup:
  - service: neon-postgres
    why: "The migration and link-generation scripts cannot run end-to-end without a live Postgres database. Provisioning Neon and supplying DATABASE_URL is a human-only action (account/dashboard)."
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console (or Vercel Marketplace → Neon integration) → connection string (use the pooled or direct connection string with ?sslmode=require)"
      - name: GUEST_TOKEN_SECRET
        source: "Already established in Phase 6 — a long random secret in .env.local"
      - name: SITE_BASE_URL
        source: "Optional. The deployed site origin. If unset, links use a clearly-marked placeholder host (regenerate after Phase 9)."
    dashboard_config:
      - task: "Create a Neon project + database, copy the connection string into .env.local as DATABASE_URL"
        location: "Neon Console (https://console.neon.tech) or Vercel → Storage → Neon"
must_haves:
  truths:
    - "Running generate-links.js against the example CSV inserts guest rows and writes links.csv with one valid personalized URL per kept row"
    - "Re-running with the same email keeps the same id (the issued link stays valid)"
    - "Removing an email from the CSV and re-running soft-deletes that DB row (deleted_at set), and its link still resolves"
    - "A URL from links.csv decodes (via the Phase 6 browser util) to the correct guest name"
    - "links.csv has exactly the columns id,display_name,email,url"
    - "Neither guests.csv nor links.csv nor .env.local appears in git status"
  artifacts:
    - path: "scripts/generate-links.js"
      provides: "CSV → normalized email upsert (preserve id) → soft-delete sync → sign token → write links.csv"
      contains: "ON CONFLICT (email) DO UPDATE"
    - path: "scripts/check-link.js"
      provides: "End-to-end: read links.csv, extract t=, decodeGuestToken, assert name matches, validate columns"
      contains: "decodeGuestToken"
  key_links:
    - from: "scripts/generate-links.js"
      to: "scripts/lib/token.js sign()"
      via: "import { sign } from './lib/token.js'"
      pattern: "import \\{ sign \\} from '\\./lib/token\\.js'"
    - from: "scripts/generate-links.js"
      to: "scripts/lib/links.js helpers"
      via: "import shapeRows/computeSoftDeletes/buildLinkUrl/normalizeEmail"
      pattern: "from '\\./lib/links\\.js'"
    - from: "scripts/check-link.js"
      to: "src/lib/decodeGuestToken.js"
      via: "import (script → src, the allowed direction)"
      pattern: "from '\\.\\./src/lib/decodeGuestToken\\.js'"
---

<objective>
Wire the proven pure helpers (07-02) and the Phase 6 `sign()` into the DB-backed `scripts/generate-links.js`, build the end-to-end `scripts/check-link.js`, and run the full chain against a real Neon database the user provisions. This produces the actual artifact the couple uses: `links.csv` of durable personalized URLs.

Purpose: This is where BACK-01 (real rows in Neon, id preserved across re-runs, soft-delete) and LINK-04 (mint links from a CSV; secret + list never committed; URL decodes to the right name) become true end-to-end.

Output: `scripts/generate-links.js`, `scripts/check-link.js`, and a verified live run producing `links.csv`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-datastore-schema-link-generation-tooling/07-CONTEXT.md
@.planning/phases/07-datastore-schema-link-generation-tooling/07-RESEARCH.md
@docs/identity-token-contract.md

<interfaces>
<!-- REUSE these — do NOT duplicate any signing, normalization, or URL logic. -->

From scripts/lib/token.js (Phase 6 — import, do NOT re-implement signing):
  export function sign(payload, secret): string   // returns `${base64urlPayload}.${base64urlHmac}`
  // payload MUST be the locked shape exactly: { id, name, iat }  — email is NEVER in the token

From scripts/lib/links.js (Plan 07-02 — pure, tested):
  export function normalizeEmail(email): string
  export function shapeRows(rawRows): { rows: [{display_name, email}], skipped: [{line, reason}] }
  export function computeSoftDeletes(activeCsvEmails, dbActiveEmails): string[]
  export function buildLinkUrl(id, token, baseUrl): string   // locked /i/<id>?t=<token>, trailing-slash safe

From src/lib/decodeGuestToken.js (Phase 6 — browser-safe, imported INTO this script; the allowed scripts→src direction):
  export function decodeGuestToken(token): { id, name } | null

Neon HTTP driver (07-RESEARCH Pattern 1/4/5):
  import { neon } from '@neondatabase/serverless';
  const sql = neon(process.env.DATABASE_URL);
  // upsert preserving id (two-step, Pattern 4):
  const existing = await sql`SELECT id FROM guests WHERE email = ${normalizedEmail}`;
  const id = existing.length > 0 ? existing[0].id : nanoid();
  await sql`INSERT INTO guests (id, display_name, email, created_at)
            VALUES (${id}, ${displayName}, ${normalizedEmail}, now())
            ON CONFLICT (email) DO UPDATE SET display_name = EXCLUDED.display_name, deleted_at = NULL`;
  // soft-delete sync (Pattern 5, array-binding-safe form):
  await sql`UPDATE guests SET deleted_at = now() WHERE email <> ALL(${activeEmails}::text[]) AND deleted_at IS NULL`;

CSV parse (07-RESEARCH Pattern 7), header-case-insensitive (Pitfall 5):
  import { parse } from 'csv-parse/sync';
  const rows = parse(raw, { columns: (h)=>h.map(x=>x.trim().toLowerCase()), skip_empty_lines: true, trim: true, bom: true });
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write generate-links.js (CSV → upsert → soft-delete → sign → links.csv)</name>
  <read_first>
    - scripts/lib/links.js (the pure helpers from 07-02 — import and reuse; do NOT re-implement)
    - scripts/lib/token.js (sign signature; import { sign } — do NOT duplicate signing)
    - .planning/phases/07-datastore-schema-link-generation-tooling/07-RESEARCH.md (Pattern 1 neon(); Pattern 4 two-step upsert preserving id; Pattern 5 soft-delete with ::text[] cast fallback; Pattern 7 csv-parse; Pattern 8 links.csv quoting; Pattern 11 token build; Anti-Patterns + Pitfalls 3/4/7)
    - .planning/phases/07-datastore-schema-link-generation-tooling/07-CONTEXT.md (D-01..D-09 — all locked)
    - docs/identity-token-contract.md (§2 payload is EXACTLY { id, name, iat }; email NEVER in token)
    - scripts/check-token-url.js (existing script style reference)
  </read_first>
  <action>
    Create `scripts/generate-links.js`. Run command (top-of-file comment): `node --env-file=.env.local scripts/generate-links.js` (or `npm run db:generate-links`). ESM, top-level await. Steps:

    1. Imports:
       import { readFileSync, writeFileSync } from 'node:fs';
       import { parse } from 'csv-parse/sync';
       import { neon } from '@neondatabase/serverless';
       import { nanoid } from 'nanoid';
       import { sign } from './lib/token.js';
       import { shapeRows, computeSoftDeletes, buildLinkUrl } from './lib/links.js';

    2. Fail fast (console.error + process.exit(1)) if either is unset:
       - process.env.DATABASE_URL → "DATABASE_URL is not set. Provision Neon and add it to .env.local."
       - process.env.GUEST_TOKEN_SECRET → "GUEST_TOKEN_SECRET is not set in .env.local (established in Phase 6)."

    3. Read the CSV. Default input path `guests.csv` at repo root; allow override via `process.argv[2]`. Parse with:
       parse(raw, { columns: (h)=>h.map(x=>x.trim().toLowerCase()), skip_empty_lines: true, trim: true, bom: true })
       (header-case-insensitive per Pitfall 5).

    4. `const { rows, skipped } = shapeRows(parsed);`  Print each skipped entry as a friendly warning: `console.warn(\`Skipping line ${s.line}: ${s.reason}\`);` (D-03 — continue, do not abort).

    5. `const sql = neon(process.env.DATABASE_URL);`  `const secret = process.env.GUEST_TOKEN_SECRET;`  `const out = [];`

    6. For each shaped row (email already normalized by shapeRows):
       - Two-step upsert preserving id (Pattern 4):
         const existing = await sql`SELECT id FROM guests WHERE email = ${row.email}`;
         const id = existing.length > 0 ? existing[0].id : nanoid();
         await sql`INSERT INTO guests (id, display_name, email, created_at)
                   VALUES (${id}, ${row.display_name}, ${row.email}, now())
                   ON CONFLICT (email) DO UPDATE SET display_name = EXCLUDED.display_name, deleted_at = NULL`;
       - Build token with the LOCKED payload shape ONLY (email is NEVER included):
         const token = sign({ id, name: row.display_name, iat: Math.floor(Date.now()/1000) }, secret);
       - const url = buildLinkUrl(id, token, process.env.SITE_BASE_URL);
       - out.push({ id, display_name: row.display_name, email: row.email, url });

    7. Soft-delete sync AFTER the loop (D-05, Pattern 5). Get DB-active emails and diff via the pure helper, OR run the SQL directly with the array-binding-safe form (include the ::text[] cast per RESEARCH Open Question 1):
         const activeEmails = out.map(r => r.email);
         await sql`UPDATE guests SET deleted_at = now() WHERE email <> ALL(${activeEmails}::text[]) AND deleted_at IS NULL`;
       (If activeEmails is empty, this soft-deletes all active rows — that is the correct "CSV emptied" behavior. Guard only if you want to refuse an empty CSV; default behavior is acceptable.)

    8. Write links.csv with EXACT column order `id,display_name,email,url` using a 6-line CSV-escape helper (Pattern 8 — wrap values containing comma/quote/newline in double quotes, escape inner quotes by doubling). Do NOT add a second csv package.
       Header line: `id,display_name,email,url`

    9. Friendly summary to stdout: number written, number skipped, output path; and if SITE_BASE_URL was unset, warn that links use a placeholder host and must be regenerated once the Phase 9 domain is known.

    HARD RULES:
    - Do NOT put email in the token payload (locked { id, name, iat }).
    - Do NOT set first_seen_at (Phase 8 — Pitfall 7).
    - Do NOT re-implement sign() or URL building — import them.
    - Do NOT use Pool — use neon() HTTP driver.
  </action>
  <verify>
    <automated>node --check scripts/generate-links.js && grep -q "import { sign } from './lib/token.js'" scripts/generate-links.js && grep -q "ON CONFLICT (email) DO UPDATE" scripts/generate-links.js && grep -q "id,display_name,email,url" scripts/generate-links.js && grep -q "neon(process.env.DATABASE_URL)" scripts/generate-links.js && echo "STRUCTURE OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/generate-links.js` exits 0
    - Contains `import { sign } from './lib/token.js'` (reuses Phase 6 signing — no duplication)
    - Contains `from './lib/links.js'` (reuses 07-02 pure helpers)
    - Contains `ON CONFLICT (email) DO UPDATE`
    - Contains a SELECT of existing id by email AND `nanoid()` (two-step id preservation)
    - Contains `<> ALL(` and `deleted_at = now()` (soft-delete sync)
    - Contains the literal header `id,display_name,email,url`
    - Contains `neon(process.env.DATABASE_URL)` and fail-fast guards for BOTH `DATABASE_URL` and `GUEST_TOKEN_SECRET`
    - `grep -n "name:" scripts/generate-links.js` shows the sign payload uses `{ id, name:`; `grep "email" ` near the sign() call shows email is NOT a payload key
    - `grep "first_seen_at" scripts/generate-links.js` produces NO output
    - `grep -i "Pool" scripts/generate-links.js` produces NO output
  </acceptance_criteria>
  <done>scripts/generate-links.js parses; reuses sign() + the pure helpers; performs id-preserving email upsert, soft-delete sync, and locked-payload signing; writes links.csv with the exact 4 columns; fails fast on missing env; never touches first_seen_at or puts email in the token.</done>
</task>

<task type="auto">
  <name>Task 2: Write check-link.js (end-to-end URL → decode → name assertion + column validation)</name>
  <read_first>
    - scripts/check-token-url.js (template: ESM, new URL(...).searchParams.get('t'), decodeGuestToken, PASS/FAIL console + process.exit)
    - src/lib/decodeGuestToken.js (the browser-safe util this script imports INTO Node — returns { id, name } | null; uses atob, Node 18+)
    - .planning/phases/07-datastore-schema-link-generation-tooling/07-RESEARCH.md (Pattern 10 end-to-end check; Pitfall 2 atob Node 18+; the LINK-04 column-validation row in the test map)
  </read_first>
  <action>
    Create `scripts/check-link.js`. Run command (top comment): `node scripts/check-link.js` (NO --env-file — decodeGuestToken uses no secret; Node 18+ required for atob). Steps:

    1. Imports:
       import { readFileSync } from 'node:fs';
       import { parse } from 'csv-parse/sync';
       import { decodeGuestToken } from '../src/lib/decodeGuestToken.js';
       (This scripts→src import direction is allowed; NEVER the reverse.)

    2. Read `links.csv` (path `links.csv` at repo root, override via process.argv[2]). Parse with `{ columns: true }`.
       Fail fast (console.error + exit 1) if the file is missing or has zero data rows, with: "links.csv not found or empty — run: npm run db:generate-links first".

    3. Validate columns: assert the parsed header keys are exactly `id`, `display_name`, `email`, `url` (LINK-04 column check). On mismatch print expected vs got and exit 1.

    4. Take the first data row. Extract the token:
       const token = new URL(row.url).searchParams.get('t');
       const decoded = decodeGuestToken(token);
    5. Assert `decoded !== null` and `decoded.name === row.display_name` and `decoded.id === row.id`.
       On pass: `console.log(\`PASS: "${decoded.name}" (id ${decoded.id}) decoded from links.csv URL\`); process.exit(0);`
       On fail: print the mismatch and `process.exit(1)`.

    NOTE: This requires a generated `links.csv` (from Task 1's live run, gated by the Task 3 checkpoint). Authoring + `node --check` is verifiable now; the live PASS is verified after the checkpoint.
  </action>
  <verify>
    <automated>node --check scripts/check-link.js && grep -q "from '../src/lib/decodeGuestToken.js'" scripts/check-link.js && grep -q "searchParams.get('t')" scripts/check-link.js && echo "STRUCTURE OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/check-link.js` exits 0
    - Contains `import { decodeGuestToken } from '../src/lib/decodeGuestToken.js'` (scripts→src, allowed direction)
    - Contains `new URL(` and `searchParams.get('t')`
    - Contains an assertion comparing `decoded.name` to the row's `display_name`
    - Contains a column-validation check for `id`, `display_name`, `email`, `url`
    - `grep -E "process.env|GUEST_TOKEN_SECRET|DATABASE_URL" scripts/check-link.js` produces NO output (no secret needed)
    - `grep -i "scripts/lib/token" scripts/check-link.js` produces NO output (must NOT import the secret-bearing lib)
  </acceptance_criteria>
  <done>scripts/check-link.js parses, reads links.csv, validates the 4 columns, decodes the first URL's token via the browser-safe util, and asserts the decoded name + id match the row — using no secret.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Provision Neon + run the live migrate → generate-links → check-link chain</name>
  <what-built>
    The full local tooling chain is authored and unit-tested: `scripts/migrate.js` (creates the guests table), `scripts/generate-links.js` (CSV → Neon upsert → links.csv), and `scripts/check-link.js` (end-to-end decode check). The pure logic is already green via `node --test scripts/generate-links.test.js`. What remains can only run against a real database.
  </what-built>
  <how-to-verify>
    This step needs a Neon Postgres database, which only you can provision (account/dashboard action). Then the agent runs the live chain.

    YOU do (one-time):
    1. Create a Neon project + database at https://console.neon.tech (or via Vercel → Storage → Neon).
    2. Copy the connection string into `.env.local` at the repo root as:
       DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require
    3. Confirm `.env.local` already has GUEST_TOKEN_SECRET (from Phase 6). Optionally add SITE_BASE_URL.
    4. Tell the agent the DB is ready.

    THEN the agent runs (you watch the output):
    a. `cp scripts/guests.example.csv guests.csv` (a safe, gitignored test list)
    b. `npm run db:migrate` → expect "Migration complete: guests table ready." (run it TWICE to prove idempotency — second run must also succeed with no error)
    c. `npm run db:generate-links` → expect a per-row summary, blank-name warnings if any, and `links.csv` written
    d. `node scripts/check-link.js` → expect `PASS: "<name>" decoded from links.csv URL`
    e. Re-run idempotency/upsert proof: `npm run db:generate-links` again → confirm the same ids persist for unchanged emails (the id column in links.csv is unchanged for the same email)
    f. `git status --short` → confirm NONE of `guests.csv`, `links.csv`, `.env.local` appear

    Expected end state: `links.csv` exists with columns id,display_name,email,url; check-link.js prints PASS; git status is clean of all private files.
  </how-to-verify>
  <resume-signal>Type "approved" once Neon is provisioned and DATABASE_URL is in .env.local (or report any errors from the live run).</resume-signal>
  <action>BLOCKING checkpoint: wait for the user to provision Neon and add DATABASE_URL to .env.local (see how-to-verify). Once they confirm, the agent runs the live chain: cp scripts/guests.example.csv guests.csv; npm run db:migrate (twice, idempotent); npm run db:generate-links; node scripts/check-link.js; re-run generate-links to prove ids persist; git status --short to confirm guests.csv/links.csv/.env.local are not staged.</action>
  <verify>node scripts/check-link.js prints PASS; npm run db:migrate run twice both exit 0; git status --short shows no guests.csv, links.csv, or .env.local</verify>
  <done>Neon guests table populated; links.csv generated with columns id,display_name,email,url; check-link.js PASS; re-run preserves ids; no private files staged in git.</done>
</task>

</tasks>

<verification>
- `node --check` passes for both new scripts (Tasks 1 & 2)
- generate-links.js reuses sign() and the pure helpers (grep), uses ON CONFLICT (email) DO UPDATE + soft-delete, writes the 4-column links.csv
- check-link.js imports decodeGuestToken from src (allowed direction), validates columns, asserts name match
- LIVE (after checkpoint): migrate runs twice cleanly; generate-links writes links.csv; check-link.js prints PASS; re-run preserves ids; `git status --short` shows no guests.csv / links.csv / .env.local
</verification>

<success_criteria>
- `scripts/generate-links.js` mints durable links from a CSV using id-preserving email upsert + soft-delete, signing via Phase 6 sign() (BACK-01, LINK-04)
- `scripts/check-link.js` proves a generated URL decodes to the correct guest name (ROADMAP success criterion #3, LINK-04)
- Live run: guests table populated in Neon; links.csv produced; re-run keeps ids; private files never staged (ROADMAP success criteria #2 and #4)
</success_criteria>

<output>
After completion, create `.planning/phases/07-datastore-schema-link-generation-tooling/07-03-SUMMARY.md`
</output>
