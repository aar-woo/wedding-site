---
phase: 07-datastore-schema-link-generation-tooling
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/lib/links.js
  - scripts/generate-links.test.js
autonomous: true
requirements: [BACK-01, LINK-04]
must_haves:
  truths:
    - "Blank-display_name CSV rows are skipped and reported by line number (run does not abort)"
    - "Email keying is normalized (trim + lowercase) so casing differences never create duplicate rows"
    - "The set of rows to soft-delete is computed correctly: DB emails not present in the current CSV"
    - "A link URL is assembled as the locked /i/<id>?t=<token> with no double-slash, using a placeholder host when SITE_BASE_URL is unset"
    - "These behaviors are provable WITHOUT a live database (pure functions, unit-tested)"
  artifacts:
    - path: "scripts/lib/links.js"
      provides: "Pure, DB-free helpers: row shaping, email normalization, soft-delete diff, URL build, blank-name skip"
      exports: ["normalizeEmail", "shapeRows", "computeSoftDeletes", "buildLinkUrl"]
    - path: "scripts/generate-links.test.js"
      provides: "node:test unit suite covering blank-name skip, normalization, soft-delete diff, URL build, upsert-id selection"
      contains: "node:test"
  key_links:
    - from: "scripts/lib/links.js"
      to: "buildLinkUrl"
      via: "locked URL shape /i/<id>?t=<token>"
      pattern: "/i/\\$\\{|/i/'"
    - from: "scripts/generate-links.test.js"
      to: "scripts/lib/links.js"
      via: "import"
      pattern: "from '\\./lib/links\\.js'"
---

<objective>
Extract the DB-free logic of link generation into pure, unit-testable helper functions (`scripts/lib/links.js`) and write the `node:test` suite that proves them — BEFORE the DB-wired `generate-links.js` is built (plan 07-03). This is the heart of the Nyquist validation: the upsert/soft-delete/skip/URL logic is provable with zero database.

Purpose: The CONTEXT decisions (blank-name skip D-03, email-keyed upsert D-04, soft-delete sync D-05, configurable base URL D-07) all contain real logic that would otherwise only be testable against a live Neon DB. Pulling them into pure functions makes them fast-tested and lets plan 07-03's `generate-links.js` be a thin DB-wiring layer over already-proven logic.

Output: `scripts/lib/links.js` (pure helpers, no DB import) + `scripts/generate-links.test.js` (node:test, no DB needed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-datastore-schema-link-generation-tooling/07-CONTEXT.md
@.planning/phases/07-datastore-schema-link-generation-tooling/07-RESEARCH.md

<interfaces>
<!-- Mirror node:test patterns from the EXISTING suite scripts/lib/token.test.js: -->
  import { test } from 'node:test';
  import assert from 'node:assert/strict';

<!-- Helper contracts to CREATE in scripts/lib/links.js. These are the contracts the test -->
<!-- file and plan 07-03's generate-links.js build against. NO database import in this file. -->

  // Trim + lowercase. Used everywhere an email is read/keyed (RESEARCH Pitfall 3).
  export function normalizeEmail(email: string): string

  // Take raw parsed CSV rows [{ display_name, email }, ...].
  // - Skip rows with blank/whitespace-only display_name; collect a warning per skip (with 1-based line number).
  // - Normalize email on kept rows.
  // Returns { rows: Array<{ display_name, email }>, skipped: Array<{ line: number, reason: string }> }
  export function shapeRows(rawRows: Array<{display_name, email}>): { rows, skipped }

  // Given the set of normalized emails active in this CSV run and the list of
  // normalized emails currently NOT-soft-deleted in the DB, return the emails to soft-delete
  // (present in DB-active list, absent from the CSV).
  export function computeSoftDeletes(activeCsvEmails: string[], dbActiveEmails: string[]): string[]

  // Build the locked URL: `${base}/i/${id}?t=${token}`, stripping any trailing slash on base.
  // If base is falsy, use a clearly-marked placeholder host.
  export function buildLinkUrl(id: string, token: string, baseUrl: string | undefined): string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing node:test suite for the pure link helpers (RED)</name>
  <read_first>
    - scripts/lib/token.test.js (the established node:test pattern to MIRROR: `import { test } from 'node:test'`, `import assert from 'node:assert/strict'`, one behavior per test())
    - .planning/phases/07-datastore-schema-link-generation-tooling/07-CONTEXT.md (D-03 blank-name skip + line number; D-04 upsert keyed on normalized email; D-05 soft-delete = in DB not in CSV; D-07 SITE_BASE_URL + placeholder)
    - .planning/phases/07-datastore-schema-link-generation-tooling/07-RESEARCH.md (Pitfall 3 email normalization; Pitfall 4 trailing slash; Pitfall 5 header casing)
  </read_first>
  <behavior>
    - normalizeEmail("  Foo@Bar.COM ") === "foo@bar.com"
    - shapeRows: a row with display_name "" or "   " is excluded from .rows and recorded in .skipped with a 1-based line number (header is line 1, so first data row is line 2)
    - shapeRows: a valid row's email is normalized in the returned .rows
    - shapeRows: a blank-name row in the MIDDLE does not drop later valid rows (run continues)
    - computeSoftDeletes(["a@x.com"], ["a@x.com","b@x.com"]) === ["b@x.com"]  (b is in DB-active but not in CSV)
    - computeSoftDeletes(["a@x.com"], []) === []  (nothing in DB to delete)
    - computeSoftDeletes([], ["a@x.com"]) === ["a@x.com"]  (CSV emptied → all DB-active soft-deleted)
    - buildLinkUrl("ID1","tok","https://x.com") === "https://x.com/i/ID1?t=tok"
    - buildLinkUrl("ID1","tok","https://x.com/") === "https://x.com/i/ID1?t=tok"  (no double slash — Pitfall 4)
    - buildLinkUrl("ID1","tok", undefined) contains "/i/ID1?t=tok" AND contains a clearly-marked placeholder host (assert it contains "PLACEHOLDER" or "REPLACE" case-insensitively)
  </behavior>
  <action>
    Create `scripts/generate-links.test.js` using node:test (mirror scripts/lib/token.test.js exactly for structure). Import the helpers: `import { normalizeEmail, shapeRows, computeSoftDeletes, buildLinkUrl } from './lib/links.js';` Write one `test(...)` per behavior listed above using `assert` from `node:assert/strict` (use `assert.equal` for scalars, `assert.deepEqual` for arrays). Do NOT import any DB driver, neon, or token.js — these are pure-logic tests only. At this point `scripts/lib/links.js` does not exist yet, so the suite MUST fail (RED).
  </action>
  <verify>
    <automated>node --check scripts/generate-links.test.js && echo "TEST FILE PARSES (red expected at runtime until Task 2)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/generate-links.test.js` exists and `node --check` on it exits 0
    - File contains `import { test } from 'node:test'` and `import assert from 'node:assert/strict'`
    - File contains `from './lib/links.js'`
    - File contains at least one test referencing each of: `normalizeEmail`, `shapeRows`, `computeSoftDeletes`, `buildLinkUrl`
    - File contains an assertion for the trailing-slash case (no double slash)
    - `grep -iE "neon|@neondatabase|token\\.js" scripts/generate-links.test.js` produces NO output (no DB / no signing in unit tests)
    - Running `node --test scripts/generate-links.test.js` FAILS at this point (links.js missing) — RED confirmed
  </acceptance_criteria>
  <done>The test file parses, mirrors the token.test.js node:test style, covers all four helpers with the listed cases, imports no DB/crypto, and fails because links.js does not yet exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure link helpers to pass the suite (GREEN)</name>
  <read_first>
    - scripts/generate-links.test.js (the suite written in Task 1 — implement exactly to these expectations)
    - scripts/lib/token.js (sibling module style in scripts/lib/: ESM named exports, JSDoc; do NOT import it here — links.js stays crypto-free and DB-free)
    - .planning/phases/07-datastore-schema-link-generation-tooling/07-RESEARCH.md (Pattern 5 soft-delete semantics; Pitfall 4 trailing-slash strip; §Pattern 11 placeholder host string)
  </read_first>
  <action>
    Create `scripts/lib/links.js` with four pure named exports (no DB import, no crypto import):

    1. `export function normalizeEmail(email) { return String(email).trim().toLowerCase(); }`

    2. `export function shapeRows(rawRows)`:
       - Iterate rawRows with index i. The CSV header is line 1, so the line number for rawRows[i] is `i + 2`.
       - If `display_name` is missing or `String(display_name).trim() === ''`, push `{ line: i + 2, reason: 'blank display_name' }` to a `skipped` array and continue (do NOT throw, do NOT abort).
       - Otherwise push `{ display_name: row.display_name.trim ? row.display_name.trim() : row.display_name, email: normalizeEmail(row.email) }` to a `rows` array. (Keep display_name as-is aside from trim; unicode/& must survive.)
       - Return `{ rows, skipped }`.

    3. `export function computeSoftDeletes(activeCsvEmails, dbActiveEmails)`:
       - Normalize both inputs through normalizeEmail (defensive).
       - Build a Set from activeCsvEmails.
       - Return dbActiveEmails.filter(e => !csvSet.has(e)).  (Emails active in DB but absent from CSV → soft-delete.)

    4. `export function buildLinkUrl(id, token, baseUrl)`:
       - `const PLACEHOLDER = 'https://REPLACE-ME-SET-SITE_BASE_URL.example';`
       - `const base = (baseUrl || PLACEHOLDER).replace(/\/+$/, '');`  (strip trailing slash(es) — Pitfall 4)
       - `return \`${base}/i/${id}?t=${token}\`;`

    Add a top-of-file JSDoc note: "Pure, DB-free, crypto-free helpers for link generation. The DB wiring and token signing live in scripts/generate-links.js (plan 07-03)."
  </action>
  <verify>
    <automated>node --test scripts/generate-links.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test scripts/generate-links.test.js` exits 0 with all tests passing (GREEN)
    - `scripts/lib/links.js` exports `normalizeEmail`, `shapeRows`, `computeSoftDeletes`, `buildLinkUrl`
    - `scripts/lib/links.js` contains `/i/` and `?t=` (locked URL shape)
    - `scripts/lib/links.js` contains a trailing-slash strip (e.g. `.replace(/\/+$/`)
    - `grep -iE "neon|@neondatabase|createHmac|require\\(|token\\.js" scripts/lib/links.js` produces NO output (pure module — no DB, no crypto)
    - `node --test scripts/lib/token.test.js` still exits 0 (no regression to existing suite)
  </acceptance_criteria>
  <done>scripts/lib/links.js implements all four helpers as pure functions; the full unit suite is green; no DB or crypto imported; existing token tests still pass.</done>
</task>

</tasks>

<verification>
- RED then GREEN: Task 1 leaves a failing suite; Task 2 makes `node --test scripts/generate-links.test.js` exit 0
- `node --test scripts/lib/token.test.js` still passes (no regression)
- links.js imports nothing from a DB driver or crypto (grep clean)
</verification>

<success_criteria>
- `scripts/lib/links.js` exists with four pure exports proven by tests (BACK-01 upsert-id-selection + soft-delete logic; LINK-04 blank-name skip + URL build)
- `scripts/generate-links.test.js` covers blank-name skip, email normalization, soft-delete diff, and URL build — all without a database
- Full unit suite green; no DB required to run it
</success_criteria>

<output>
After completion, create `.planning/phases/07-datastore-schema-link-generation-tooling/07-02-SUMMARY.md`
</output>
