---
phase: 09-mobile-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/SaveTheDatePage.module.css
  - src/components/CountdownTimer.module.css
autonomous: true
requirements: [EXP-01]
must_haves:
  truths:
    - "On a 375x667 viewport (iPhone SE), the entire keepsake is visible at once with no vertical or horizontal scroll"
    - "Save-the-Date label, couple names, divider, date, location, countdown, footer, and both botanicals are all on screen, none clipped"
    - "The countdown row (4 units + 3 gaps) does not crowd or overflow the 375px width"
  artifacts:
    - path: "src/pages/SaveTheDatePage.module.css"
      provides: "Tightened base-block spacing so topGroup, botanicalPair, and contentBlock fit one 375px screen"
      contains: ".contentBlock"
    - path: "src/components/CountdownTimer.module.css"
      provides: "Countdown row that fits within 375px"
      contains: ".countdown"
  key_links:
    - from: ".topGroup (top: 20px)"
      to: ".botanicalPair (bottom offset) / .contentBlock (bottom: 0)"
      via: "vertical space budget on a 667px-tall viewport — must not collide"
      pattern: "bottom:"
---

<objective>
Make the existing mobile-first save-the-date layout fit a 375x667px viewport (iPhone SE floor, D-03) as one screen with no scroll, overflow, or clipping — delivering EXP-01.

Purpose: The single-glance personalized reveal is the core value. On short viewports the three independently-anchored groups (`.topGroup` top-pinned, `.botanicalPair` at `bottom: 180px`, `.contentBlock` bottom-pinned) can collide and push content off-screen. This is verify-and-fix only (D-01): tighten sizes/spacing in the BASE CSS block; do not redesign and do not touch the desktop `@media (min-width: 768px)` block (locked, D-02).

Output: Tightened base-block CSS in `SaveTheDatePage.module.css` and (if needed) `CountdownTimer.module.css`, verified at 375x667 in DevTools.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-mobile-polish-deploy/09-CONTEXT.md
@.planning/phases/09-mobile-polish-deploy/09-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Current base-block layout values the executor is tightening. Extracted from src/pages/SaveTheDatePage.module.css. -->
<!-- DO NOT touch the @media (min-width: 768px) block (lines ~192-230) — desktop is locked. -->

The three independently-anchored groups (all position: absolute, all in the BASE block):
- .topGroup       { top: 20px;    padding: 48px 12px 0; }      /* Save the Date label, divider, couple names */
- .botanicalPair  { bottom: 180px; padding: 0 24px; }          /* two BotanicalSvg, flanks couple names */
- .contentBlock   { bottom: 0;     padding: 0 28px 48px; }     /* greeting, date, location, countdown, footer */

Current content-block element spacing (base block):
- .coupleNames { font-size: 24px; margin: 0 0 24px; }  (lives in .topGroup)
- .divider     { margin: 20px auto; }
- .date        { font-size: 28px; margin: 0 0 16px; }
- .location    { font-size: 14px; margin: 0 0 32px; }
- .footer      { font-size: 11px; margin: 0; }
- .label       { font-size: clamp(20px, 8vw, 32px); margin: 0 0 8px; }  (clamp is intentional, keep it)

From src/components/CountdownTimer.module.css:
- .countdown { gap: 20px; margin: 0 0 32px; }
- .unit      { min-width: 44px; }
- Row width math: 4*44 + 3*20 = 236px < 375px  (already fits horizontally — confirm, do not shrink units unless DevTools shows crowding)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the 375x667 layout and identify the collision/overflow</name>
  <read_first>
    - src/pages/SaveTheDatePage.module.css (full file — see current absolute-anchor values and confirm the 768px block boundary at the `@media (min-width: 768px)` line)
    - src/components/CountdownTimer.module.css (countdown row dimensions)
    - src/pages/SaveTheDatePage.jsx (so you know which elements live in .topGroup vs .contentBlock vs .botanicalPair)
    - .planning/phases/09-mobile-polish-deploy/09-RESEARCH.md (Pattern 1 + Pitfall 1 — the collision math and fix strategy)
  </read_first>
  <action>
    Do NOT edit anything in this task — this is a measurement pass that produces the concrete numbers Task 2 will apply.

    1. Start the dev server: `npm run dev` (Vite, defaults to http://localhost:5173).
    2. Reason about the vertical budget on a 375x667 viewport WITHOUT desktop styles (the 768px media query does NOT apply at 375px):
       - `.topGroup` sits at top: 20px with padding-top 48px → its content starts ~68px from the top and contains label (~32px) + divider (margin 20px + 1px + 20px) + coupleNames (~26px + margin-bottom 24px). Estimated topGroup bottom edge ≈ 68 + 32 + 41 + 50 ≈ 191px from top.
       - `.botanicalPair` sits at bottom: 180px → on a 667px viewport its baseline is at y ≈ 667 - 180 = 487px from top; the SVGs extend upward from there.
       - `.contentBlock` sits at bottom: 0 with padding-bottom 48px and contains greeting + date (28px, margin 16px) + location (14px, margin 32px) + countdown (~37px tall row, margin 32px) + footer (11px). Estimated contentBlock height ≈ 48 (pad) + 11 (footer) + 37 (countdown) + 32 + 14 + 32 + 28 + 16 + greeting(~24) ≈ 242px → its top edge ≈ 667 - 242 ≈ 425px from top.
    3. Identify the collision zone: `.botanicalPair` baseline (~487px) vs `.contentBlock` top (~425px) — the botanicals at bottom: 180px overlap the top of the content block. This is the primary fix target. Confirm in DevTools (device toolbar → 375x667 / iPhone SE): look for body scrollbar, botanical SVGs overlapping countdown/date text, or footer clipped.
    4. Record (in your working notes for Task 2) the exact observed problems: which elements overlap, whether a scrollbar appears, and the measured pixel overlap.
  </action>
  <verify>
    <automated>cd /Users/aaronwoo/Desktop/aar-woo-repos-local/wedding-site && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DevTools opened at 375x667; the specific overlapping/clipped/overflowing elements are identified and written into Task 2's working notes (which group collides with which, and by roughly how many px).
    - `npm run build` exits 0 (baseline build is healthy before edits).
    - No file was modified in this task (`git status` shows no changes to *.css).
  </acceptance_criteria>
  <done>The exact collision (botanicalPair overlapping contentBlock, and/or any scroll/clip) is identified at 375x667 with concrete pixel targets for the Task 2 edits.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten base-block spacing so everything fits one 375px screen</name>
  <read_first>
    - src/pages/SaveTheDatePage.module.css (the BASE block only — everything before `@media (min-width: 768px)`)
    - src/components/CountdownTimer.module.css
  </read_first>
  <action>
    Apply targeted CSS edits ONLY in the base block of `src/pages/SaveTheDatePage.module.css` (everything above the `@media (min-width: 768px)` line). NEVER edit inside the `@media (min-width: 768px)` block — desktop is locked (D-02). Use only CSS Module rules and existing `var(--*)` design tokens — no inline styles, no new colors/fonts (CLAUDE.md).

    Apply these tightening edits (the proven fix strategy from RESEARCH Pattern 1 / Pitfall 1). Start with the spacing reductions; only reduce font sizes as a last resort if content still does not fit:

    1. `.botanicalPair` — reduce the bottom offset so the botanicals sit beside the couple-name area and clear the content block:
       `bottom: 180px;` → `bottom: 220px;`
       (raising it OFF the content block; tune the exact value in DevTools so the botanical SVGs flank the couple names and do not overlap the date/countdown text. Acceptable range 200-240px — pick what visually clears at 375x667.)
    2. `.contentBlock` — reduce bottom padding:
       `padding: 0 28px 48px;` → `padding: 0 28px 28px;`
    3. `.location` — reduce bottom margin:
       `margin: 0 0 32px;` → `margin: 0 0 18px;`
    4. `.date` — reduce bottom margin:
       `margin: 0 0 16px;` → `margin: 0 0 12px;`
    5. `.coupleNames` — reduce bottom margin (it lives in .topGroup, shortens the header stack):
       `margin: 0 0 24px;` → `margin: 0 0 16px;`
    6. `.topGroup` — reduce top padding so the header stack starts higher:
       `padding: 48px 12px 0;` → `padding: 24px 12px 0;`
    7. In `src/components/CountdownTimer.module.css`, reduce the countdown's bottom margin so the footer isn't pushed off:
       `.countdown { ... margin: 0 0 32px; }` → `margin: 0 0 18px;`
       Do NOT shrink `.unit min-width: 44px` or `.countdown gap: 20px` unless DevTools shows the row crowding at 375px (math: 4*44 + 3*20 = 236px < 375px, so it fits — leave units alone).

    After editing, re-check at 375x667 in DevTools and adjust the `.botanicalPair bottom` value (and any one other margin) within the ranges above until: no scrollbar, no overlap, footer fully visible. Keep `clamp(20px, 8vw, 32px)` on `.label` as-is. Do not introduce a new media query.
  </action>
  <verify>
    <automated>cd /Users/aaronwoo/Desktop/aar-woo-repos-local/wedding-site && npm run build 2>&1 | tail -5 && echo "--- confirm 768px block untouched ---" && git diff src/pages/SaveTheDatePage.module.css | grep -E "^\+" | grep -i "min-width: 768px" && echo "WARNING: 768px block changed" || echo "OK: no additions inside 768px boundary line"</automated>
  </verify>
  <acceptance_criteria>
    - DevTools at 375x667 (iPhone SE): Save-the-Date label, couple names, divider, "May 30, 2027", "Oahu, Hawaii", the countdown row, "Formal invitation to follow" footer, and BOTH botanical SVGs are all visible simultaneously — no element clipped.
    - No horizontal OR vertical scrollbar on the document at 375x667 (the page is exactly one screen).
    - Botanical SVGs flank the couple-name area and do NOT overlap the date/location/countdown text.
    - `git diff src/pages/SaveTheDatePage.module.css` shows changes ONLY above the `@media (min-width: 768px)` line — zero changes to any rule inside that media query.
    - `grep -n "@media (min-width: 768px)"` still present and its body byte-for-byte unchanged (verify with `git diff` showing no `+`/`-` lines after that media query opens).
    - `npm run build` exits 0.
    - No inline style design values added; only CSS Module rules using existing `var(--*)` tokens.
  </acceptance_criteria>
  <done>The full keepsake fits one 375x667 screen with no scroll, no overlap, nothing clipped; the desktop 768px block is provably untouched; build passes.</done>
</task>

</tasks>

<verification>
- DevTools device toolbar at 375x667 (iPhone SE): all keepsake content on one screen, no horizontal/vertical scroll, nothing clipped (EXP-01 success criterion #1).
- Countdown row fits 375px (4*44 + 3*20 = 236px math, confirmed visually no crowding).
- `git diff` proves the `@media (min-width: 768px)` block is unchanged.
- `npm run build` exits 0.
</verification>

<success_criteria>
On a 375px-wide mobile viewport, all content is legible, nothing overflows or is clipped, and the countdown is readable without horizontal scrolling — EXP-01 satisfied. Desktop greeting-card view (768px block) provably unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/09-mobile-polish-deploy/09-01-SUMMARY.md`
</output>
