---
phase: 09-mobile-polish-deploy
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - .env.local
  - links.csv
autonomous: false
requirements: [DEPLOY-01]
user_setup:
  - service: vercel
    why: "Host the SPA build + /api serverless functions live on a Git-connected vercel.app URL (DEPLOY-01)"
    env_vars:
      - name: GUEST_TOKEN_SECRET
        source: "Copy from local .env.local — Vercel Dashboard > Project > Settings > Environment Variables (Production, Sensitive). NEVER VITE_-prefixed."
      - name: SITE_BASE_URL
        source: "Set to the live https://<project>.vercel.app domain — Vercel Dashboard > Settings > Environment Variables (Production)."
    dashboard_config:
      - task: "Create a Git-connected Vercel project importing aar-woo/wedding-site; Vercel auto-detects Vite (build vite build, output dist); set Node.js version to 22 LTS"
        location: "Vercel Dashboard > Add New Project > Import Git Repository"
      - task: "Connect Neon's Vercel integration to auto-inject DATABASE_URL into Production (do NOT enter DATABASE_URL by hand)"
        location: "Neon Console > Integrations > Add Vercel"
      - task: "After env vars are set, trigger a redeploy so the running deployment picks them up"
        location: "Vercel Dashboard > Deployments > Redeploy (or push to main)"
  - service: neon
    why: "Production DATABASE_URL for api/guest/[id].js (Neon Postgres provisioned in Phase 7)"
    dashboard_config:
      - task: "Link the existing Neon project to the Vercel project via the Neon-managed Vercel integration"
        location: "Neon Console > Integrations > Add Vercel > Link Existing Neon Account"
must_haves:
  truths:
    - "The site is live at a *.vercel.app URL served from a Git-connected Vercel project"
    - "Production has GUEST_TOKEN_SECRET and SITE_BASE_URL set (no VITE_ prefix) and DATABASE_URL injected via Neon integration"
    - "links.csv URLs point at the live production domain, not a placeholder/localhost host"
    - "A cold deep-link /i/:id returns 200 and serves the SPA (not a 404), the greeting renders from ?t=, and GET /api/guest/:id returns 200 with guest JSON"
  artifacts:
    - path: "links.csv"
      provides: "Production guest links regenerated against the live SITE_BASE_URL (D-12)"
      contains: "vercel.app"
    - path: ".env.local"
      provides: "SITE_BASE_URL set to the live domain for the regeneration run"
      contains: "SITE_BASE_URL"
  key_links:
    - from: "Vercel production env (DATABASE_URL via Neon integration)"
      to: "api/guest/[id].js"
      via: "serverless function reads process.env.DATABASE_URL at runtime"
      pattern: "DATABASE_URL"
    - from: "SITE_BASE_URL=live domain"
      to: "links.csv"
      via: "npm run db:generate-links rewrites URLs against the live host"
      pattern: "SITE_BASE_URL"
---

<objective>
Deploy the site live to a Git-connected Vercel project (SPA build + /api serverless functions + Neon), configure production env vars, regenerate `links.csv` against the live domain, and verify the full v2.0 stack end-to-end with one real guest link — delivering DEPLOY-01 and resolving Phase 8's deferred cold-deep-link check.

Purpose: This is the ship. The dev-side work (Plans 01 + 02) is committed first; this plan crosses the hard user-action boundary (creating a Vercel project, setting dashboard env vars, connecting Neon) that the executor cannot perform. The plan automates everything automatable (git push, the static secret audit, link regeneration, curl smoke checks) and PAUSES at explicit checkpoints for the user-only steps.

Output: A live `*.vercel.app` deployment, production-correct `links.csv`, and three passing production checks (cold deep-link 200, greeting renders, API 200).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-mobile-polish-deploy/09-CONTEXT.md
@.planning/phases/09-mobile-polish-deploy/09-RESEARCH.md
@CLAUDE.md
@docs/identity-token-contract.md
@vercel.json

<interfaces>
<!-- Deploy facts (RESEARCH Pattern 3/4). vercel.json is LOCKED — do not modify. -->
<!-- Git remote: https://github.com/aar-woo/wedding-site.git (origin). -->
<!-- package.json scripts: build => "vite build"; db:generate-links => "node --env-file=.env.local scripts/generate-links.js" -->
<!-- package.json has "type": "module" — api/guest/[id].js uses `export default` (ESM, correct). -->

vercel.json (LOCKED — order matters: /api passthrough first, then SPA catch-all):
  { "rewrites": [
      { "source": "/api/(.*)", "destination": "/api/$1" },
      { "source": "/(.*)",     "destination": "/index.html" } ] }

Production env vars (ALL server-only, NEVER VITE_-prefixed):
  - DATABASE_URL       -> injected by Neon's Vercel integration (do not enter by hand)
  - GUEST_TOKEN_SECRET -> copy from local .env.local
  - SITE_BASE_URL      -> the live https://<project>.vercel.app domain

Production verification (D-13) — three checks, all must pass, one real links.csv row:
  (a) curl -s -o /dev/null -w "%{http_code}" "$DOMAIN/i/$ID?t=test"   -> 200 (cold deep-link serves SPA, not 404)
  (b) open "$DOMAIN/i/$ID?t=<token-from-links.csv>" in browser        -> "For [Name]" greeting renders from ?t=
  (c) curl -s "$DOMAIN/api/guest/$ID"                                  -> 200 + {"id":"...","displayName":"..."}
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Static secret audit + push main, then provision Vercel + Neon (USER ACTION)</name>
  <read_first>
    - .planning/phases/09-mobile-polish-deploy/09-RESEARCH.md (Pattern 3 deployment sequence steps 1-9; Pitfall 3 redeploy-after-env-vars; Pitfall 5 ESM)
    - vercel.json (confirm locked rewrite order — do NOT edit)
    - .env.example (the documented env var names; confirm none are VITE_-prefixed)
    - docs/identity-token-contract.md (§5 env vars, §7 implementation files)
  </read_first>
  <action>
    Executor automatable pre-flight BEFORE pausing (do NOT modify vercel.json):
    1. Confirm Plans 01 and 02 are committed on `main`: `git status` clean, `git log --oneline -5` shows the 09-01 and 09-02 commits. If they are missing, STOP — Wave 2 depends on them.
    2. Static secret audit (DEPLOY-01 guard — must pass before shipping):
         grep -rn "VITE_" .env* src/ | grep -iE "secret|token|database" || echo "OK: no secrets VITE-prefixed"
       Expect "OK". If any secret is VITE_-prefixed, STOP and fix before deploying.
    3. Confirm api/guest/[id].js is ESM-only (Pitfall 5):
         grep -nE "module.exports|require\(" "api/guest/[id].js" || echo "OK: ESM only"
    4. Push so Vercel can build from main:  git push origin main
    Then emit the checkpoint below and PAUSE — the user-only provisioning steps follow in <what-built>/<how-to-verify>.
  </action>
  <what-built>
    Executor automatable pre-flight (the four steps in <action> above): clean-main check, static secret audit (no VITE_-prefixed secrets), api ESM-only check, and `git push origin main` so Vercel can build from the latest commit.
  </what-built>
  <how-to-verify>
    The executor CANNOT create Vercel projects, set dashboard env vars, or connect Neon — the user does these IN ORDER (RESEARCH Pattern 3). Env vars take effect on the NEXT deploy, so a redeploy AFTER setting them is mandatory (Pitfall 3):
      a. Vercel Dashboard > Add New Project > Import Git Repository > select aar-woo/wedding-site. Vercel auto-detects Vite (build `vite build`, output `dist`). Set Node.js version to 22 LTS in Project Settings. Click Deploy.
      b. Note the assigned live domain: https://<project>.vercel.app
      c. Vercel Dashboard > Settings > Environment Variables (Production): add GUEST_TOKEN_SECRET (copy the value from local .env.local; mark Sensitive) and SITE_BASE_URL = the live domain. NEVER add a VITE_ prefix.
      d. Neon Console > Integrations > Add Vercel > link the existing Neon project to this Vercel project — auto-injects DATABASE_URL into Production. Do NOT enter DATABASE_URL by hand.
      e. Vercel Dashboard > Deployments > Redeploy (so the env vars apply — they did NOT exist for the first build).
      f. Confirm the redeploy shows "Ready" and the homepage https://<project>.vercel.app loads the save-the-date page.
  </how-to-verify>
  <verify>
    <automated>cd /Users/aaronwoo/Desktop/aar-woo-repos-local/wedding-site && (grep -rn "VITE_" .env* src/ | grep -iE "secret|token|database" || echo "OK: no secrets VITE-prefixed") && (grep -nE "module.exports|require\(" "api/guest/[id].js" || echo "OK: api ESM only") && git log --oneline -3</automated>
  </verify>
  <resume-signal>Reply with the live `*.vercel.app` domain and confirm the redeploy succeeded (status "Ready", homepage loads). Then the executor proceeds to Task 2.</resume-signal>
  <done>Static secret audit passes (no VITE_-prefixed secrets), api is ESM-only, main is pushed; user has created the Git-connected Vercel project, set GUEST_TOKEN_SECRET + SITE_BASE_URL, connected Neon (DATABASE_URL injected), redeployed, and reported the live domain.</done>
</task>

<task type="auto">
  <name>Task 2: Set SITE_BASE_URL to the live domain and regenerate production links.csv</name>
  <read_first>
    - .env.local (current SITE_BASE_URL value — placeholder/unset from Phase 7, D-07)
    - .env.example (the expected SITE_BASE_URL key shape)
    - scripts/generate-links.js (how SITE_BASE_URL is consumed when building the URL column)
    - .planning/phases/09-mobile-polish-deploy/09-RESEARCH.md (Pitfall 4 — stale SITE_BASE_URL produces broken localhost links; the verified regeneration command)
  </read_first>
  <action>
    The live domain from Task 1 now exists. Regenerate `links.csv` so guest URLs point at production (D-12, NON-NEGOTIABLE — Phase 7 links used a placeholder host and are not shippable).

    1. In `.env.local`, set the SITE_BASE_URL line to the live domain reported in Task 1 (no trailing slash):
         SITE_BASE_URL=https://<project>.vercel.app
       (Edit the existing key in place — do not duplicate it. `.env.local` is gitignored; do NOT commit it.)
    2. Ensure the source guest list exists locally. `guests.csv` is gitignored and may or may not be present (RESEARCH Environment Availability). If the link-generation script needs it and it is absent, STOP and ask the user to place the guest CSV — do not fabricate guest data.
    3. Regenerate (the script reads SITE_BASE_URL via `node --env-file=.env.local`):
         npm run db:generate-links
    4. Confirm the regenerated `links.csv` URLs use the live host and NOT localhost/placeholder:
         head -2 links.csv
       The URL column of the first data row must start with `https://<project>.vercel.app/i/`.
    5. Do NOT commit `links.csv` or `.env.local` (both gitignored, contain guest data / are environment-local). Verify they are not staged: `git status --porcelain | grep -E "links.csv|.env.local"` should return nothing tracked.
  </action>
  <verify>
    <automated>cd /Users/aaronwoo/Desktop/aar-woo-repos-local/wedding-site && grep -E "^SITE_BASE_URL=https://.*vercel\.app" .env.local && head -2 links.csv && grep -E "vercel\.app/i/" links.csv | head -1 && (git status --porcelain | grep -E "links.csv|\.env\.local" && echo "ERROR: gitignored file tracked" || echo "OK: links.csv/.env.local untracked")</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^SITE_BASE_URL=" .env.local` shows the live `https://<project>.vercel.app` domain (not localhost, not a placeholder, no trailing slash).
    - `head -2 links.csv`: the first data row's URL column starts with `https://<project>.vercel.app/i/` — confirmed via `grep -E "vercel\.app/i/" links.csv` returning at least one match.
    - No `localhost` or placeholder host remains in any links.csv URL: `grep -c "localhost" links.csv` returns 0.
    - `links.csv` and `.env.local` are NOT staged/tracked (`git status --porcelain` shows neither) — gitignore still covers both.
  </acceptance_criteria>
  <done>links.csv is regenerated with production URLs pointing at the live vercel.app domain; no localhost remains; gitignored files stay untracked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Production end-to-end verification with one real links.csv row (D-13)</name>
  <read_first>
    - links.csv (pick one real data row: its id and full /i/<id>?t=<token> URL)
    - .planning/phases/09-mobile-polish-deploy/09-RESEARCH.md (Pattern 4 — the three D-13 checks and the verified curl commands)
    - api/guest/[id].js (the endpoint shape returning { id, displayName })
  </read_first>
  <action>
    Executor runs the two automatable smoke checks (a) and (c) against the live deployment using ONE real row from the regenerated links.csv, then pauses for the user to do the browser visual check (b). Set DOMAIN to the live domain and ID/token from the chosen row:

      DOMAIN=https://<project>.vercel.app
      ID=<id from the chosen links.csv row>

    Check (a) — cold deep-link serves the SPA, not a 404 (Phase 8's deferred criterion #4):
      curl -s -o /dev/null -w "%{http_code}" "$DOMAIN/i/$ID?t=test"   -> expect 200
    Check (c) — API reads Neon and returns the guest:
      curl -s "$DOMAIN/api/guest/$ID"   -> expect HTTP 200 + {"id":"...","displayName":"..."}

    If (c) returns 500 with "DATABASE_URL undefined" / "neon connection string undefined", env vars were set but the deployment was not redeployed after — return to Task 1 step (e) and redeploy (Pitfall 3). Then emit the checkpoint below for the user's browser check.
  </action>
  <what-built>
    Executor ran smoke checks (a) cold deep-link `/i/$ID?t=test` => 200 and (c) `/api/guest/$ID` => 200 + guest JSON, against the live domain using one real links.csv row.
  </what-built>
  <how-to-verify>
    User performs check (b) — the visual greeting (a browser-only, end-to-end render the executor cannot do):
      1. Open the FULL real URL from the chosen links.csv row (including `?t=<token>`) in a browser on the live domain:
         https://<project>.vercel.app/i/<id>?t=<token-from-links.csv>
      2. Confirm the page shows "For [the correct guest name]" — NOT "For Our Beloved Guests" (which would mean the token failed to decode).
      3. Confirm the full keepsake renders (couple names, date, location, countdown, botanicals) — sanity that the production build serves correctly.
    All three D-13 checks must pass to satisfy DEPLOY-01 success criterion #4: (a) curl => 200, (c) curl => 200 + guest JSON, (b) browser => correct "For [Name]".
  </how-to-verify>
  <verify>
    <automated>echo "Smoke checks are run live against the deployed domain inside the task (curl a + c); browser greeting (b) is user-verified. No local automated command — production domain only exists post-deploy."</automated>
  </verify>
  <resume-signal>Reply "verified" once (a) returns 200, (c) returns 200 + the correct guest JSON, and (b) shows "For [correct name]" in the browser. If any check fails, describe the failure (status code / response body / what the browser showed) so the executor can diagnose (most likely: redeploy after env vars, or stale links.csv).</resume-signal>
  <done>D-13 satisfied: (a) cold deep-link /i/:id => 200 (SPA, not 404), (c) GET /api/guest/:id => 200 + correct guest JSON, (b) browser shows "For [correct name]" from the ?t= token on the live domain.</done>
</task>

</tasks>

<verification>
- `git push origin main` succeeded; static secret audit passed (no VITE_-prefixed secrets); api ESM-only.
- Live `*.vercel.app` deployment exists, Git-connected, Vite auto-detected, Node 22; GUEST_TOKEN_SECRET + SITE_BASE_URL set (no VITE_ prefix), DATABASE_URL injected via Neon integration; redeployed after env vars.
- links.csv regenerated against the live domain (no localhost remains); gitignored files untracked.
- D-13 all pass: (a) `/i/:id` curl => 200, (b) browser greeting "For [Name]" from ?t=, (c) `/api/guest/:id` curl => 200 + guest JSON.
</verification>

<success_criteria>
The site is accessible at a live `vercel.app` URL from a Git-connected Vercel project with the Neon DB integration and `GUEST_TOKEN_SECRET` configured as production env vars; at least one real generated guest link opens, shows the personalized greeting, and `GET /api/guest/:id` returns 200 — the full v2.0 stack verified end-to-end in production. DEPLOY-01 satisfied (and Phase 8's deferred cold-deep-link check resolved).
</success_criteria>

<output>
After completion, create `.planning/phases/09-mobile-polish-deploy/09-03-SUMMARY.md`
</output>
