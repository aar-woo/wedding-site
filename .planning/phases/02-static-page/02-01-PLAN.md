---
phase: 02-static-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/SaveTheDatePage.jsx
  - src/pages/SaveTheDatePage.module.css
  - src/App.jsx
autonomous: false
requirements: [HERO-01, CONT-01, CONT-02, CONT-03, CONT-04, CONT-05]

must_haves:
  truths:
    - "The hero photo fills the full viewport with no white bars or overflow"
    - '"Aaron & Rina" appears in Cormorant Garamond, gold-light color'
    - '"SAVE THE DATE" label, "May 30, 2027", and "Oahu, Hawaii" are all visible'
    - "A divider line is visible between the couple names and the date"
    - '"Formal invitation to follow" footer note appears at the bottom'
    - "All text is legible over the photo (scrim gradient darkens the bottom)"
  artifacts:
    - path: "src/pages/SaveTheDatePage.jsx"
      provides: "Hero container + scrim + bottom-anchored content block with all copy"
      contains: "Aaron &amp; Rina"
      min_lines: 25
    - path: "src/pages/SaveTheDatePage.module.css"
      provides: "Layout + typography + scrim styles, all colors via var(--*)"
      contains: "object-fit: cover"
    - path: "src/App.jsx"
      provides: "Renders SaveTheDatePage"
      contains: "SaveTheDatePage"
  key_links:
    - from: "src/App.jsx"
      to: "src/pages/SaveTheDatePage.jsx"
      via: "import + render"
      pattern: "import SaveTheDatePage"
    - from: "src/pages/SaveTheDatePage.jsx"
      to: "src/pages/SaveTheDatePage.module.css"
      via: "CSS Module import"
      pattern: "import styles"
    - from: "src/pages/SaveTheDatePage.jsx"
      to: "/images/save-the-date-hero.png"
      via: "img src"
      pattern: "/images/save-the-date-hero.png"
---

<objective>
Build the static, resting visual composition of the save-the-date page per the approved UI-SPEC: a full-bleed hero photo filling the viewport with a legibility scrim, and a bottom-anchored content block containing the section label, couple names, divider, date, location, and footer note.

Purpose: Phase 2 puts all wedding content on screen (CONT-01..05) over the hero image (HERO-01). This is the static final state — no animation, countdown, personalization, or decorations (those are Phases 3 and 4).
Output: `src/pages/SaveTheDatePage.jsx` + `.module.css`, wired into `src/App.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-page/02-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Existing foundation (Phase 1). Use these directly — no codebase exploration needed. -->

CSS custom properties already defined in src/index.css :root (use via var(--token), NEVER raw hex):
--forest: #0B1610
--gold: #BF9B5A
--gold-light: #D4B57A
--cream: #EAE0CB
--muted: #72685A
--font-display: 'Cormorant Garamond', Georgia, serif
--font-body: 'Jost', system-ui, sans-serif

src/index.css already sets: body { margin:0; background:var(--forest); color:var(--cream); font-family:var(--font-body) }
and _,_::before,\*::after { box-sizing:border-box } and #root { min-height:100svh }.

Current src/App.jsx (to be replaced in Task 2):
import styles from './App.module.css'
function App() { return (<div className={styles.app}></div>) }
export default App
NOTE: App.module.css currently centers content with flex; the page no longer needs that
shell — App becomes a thin wrapper that renders <SaveTheDatePage />.

Hero image: present at public/images/save-the-date-hero.png → reference in JSX as "/images/save-the-date-hero.png".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SaveTheDatePage component and CSS module</name>
  <files>src/pages/SaveTheDatePage.jsx, src/pages/SaveTheDatePage.module.css</files>
  <read_first>
    - .planning/phases/02-static-page/02-UI-SPEC.md (layout, typography table, scrim, copy contract)
    - CLAUDE.md (CSS Modules only, no inline design values, never system/Inter/Roboto fonts)
    - src/index.css (exact token names: --forest, --gold, --gold-light, --cream, --muted, --font-display, --font-body)
  </read_first>
  <action>
Create `src/pages/SaveTheDatePage.jsx` and `src/pages/SaveTheDatePage.module.css`.

JSX structure (per UI-SPEC "Page structure"). Import the CSS module as `styles`:

```jsx
import styles from "./SaveTheDatePage.module.css";

function SaveTheDatePage() {
  return (
    <div className={styles.page}>
      <div className={styles.heroContainer}>
        <img
          className={styles.heroImage}
          src="/images/save-the-date-hero.png"
          alt="Rina and Aaron's wedding in Oahu, Hawaii"
        />
      </div>
      <div className={styles.scrim} />
      <div className={styles.contentBlock}>
        <p className={styles.label}>Save the Date</p>
        <h1 className={styles.coupleNames}>Aaron &amp; Rina</h1>
        <div className={styles.divider} />
        <p className={styles.date}>May 30, 2027</p>
        <p className={styles.location}>Oahu, Hawaii</p>
        <p className={styles.footer}>Formal invitation to follow</p>
      </div>
    </div>
  );
}

export default SaveTheDatePage;
```

Exact copy strings (do not alter): label "Save the Date" (CSS uppercases it), couple names "Aaron &amp; Rina" (CONT-01), date "May 30, 2027" (CONT-02), location "Oahu, Hawaii" (CONT-03), footer "Formal invitation to follow" (CONT-04).

CSS module — ALL colors/fonts via var(--\*), NO raw hex except inside the scrim gradient (the gradient uses rgba of the forest color #0B1610 per UI-SPEC, which is the one documented exception). Implement exactly:

`.page` — position: relative; min-height: 100svh; overflow: hidden; background-color: var(--forest) (HERO-01 letterbox fallback)

`.heroContainer` — position: absolute; inset: 0; z-index: 0; overflow: hidden

`.heroImage` — width: 100%; height: 100%; object-fit: cover; object-position: center; display: block (HERO-01)

`.scrim` — copy verbatim from UI-SPEC "Hero Image Overlay Contract":

```css
.scrim {
  position: absolute;
  inset: 0;
  z-index: 1;
  background: linear-gradient(
    to top,
    rgba(11, 22, 16, 0.82) 0%,
    rgba(11, 22, 16, 0.45) 40%,
    rgba(11, 22, 16, 0.2) 70%,
    transparent 100%
  );
  pointer-events: none;
}
```

`.contentBlock` — position: absolute; bottom: 0; left: 0; right: 0; z-index: 2; text-align: center; padding: 0 32px 48px (xl horizontal, 2xl bottom)

Typography (from UI-SPEC type table — exact sizes/weights/colors/tracking):

- `.label` (CONT-02): font-family: var(--font-body); font-size: 11px; font-weight: 400; line-height: 1.0; color: var(--gold); text-transform: uppercase; letter-spacing: 0.25em; margin: 0 0 8px (sm)
- `.coupleNames` (CONT-01): font-family: var(--font-display); font-size: 72px; font-weight: 300; line-height: 1.1; color: var(--gold-light); letter-spacing: 0.02em; margin: 0 0 24px (lg)
- `.divider` (CONT-05): width: 48px; height: 1px; background-color: var(--gold); margin: 24px auto (NOT an <hr>)
- `.date` (CONT-02): font-family: var(--font-display); font-size: 28px; font-weight: 400; line-height: 1.3; color: var(--cream); letter-spacing: 0.04em; margin: 0 0 16px (md)
- `.location` (CONT-03): font-family: var(--font-body); font-size: 14px; font-weight: 400; line-height: 1.5; color: var(--cream); letter-spacing: 0.12em; margin: 0 0 32px (xl)
- `.footer` (CONT-04): font-family: var(--font-body); font-size: 11px; font-weight: 300; line-height: 1.5; color: var(--muted); letter-spacing: 0.15em; margin: 0

Mobile sizes from UI-SPEC are documented but Phase 5 owns full responsiveness; keep desktop sizes here. Do NOT add Framer Motion, Ken Burns, parallax, countdown, guest name, or botanical/bracket elements — those are deferred (Phase 3/4).
</action>
<verify>
<automated>test -f src/pages/SaveTheDatePage.jsx && test -f src/pages/SaveTheDatePage.module.css && grep -q "object-fit: cover" src/pages/SaveTheDatePage.module.css && grep -q "Aaron &amp; Rina" src/pages/SaveTheDatePage.jsx && grep -q "Formal invitation to follow" src/pages/SaveTheDatePage.jsx && ! grep -Eiq "#[0-9a-f]{3,6}" src/pages/SaveTheDatePage.module.css</automated>
</verify>
<acceptance_criteria> - src/pages/SaveTheDatePage.jsx exists and contains the string `Aaron &amp; Rina`, `May 30, 2027`, `Oahu, Hawaii`, `Formal invitation to follow`, and `Save the Date` - src/pages/SaveTheDatePage.jsx references the hero via `src="/images/save-the-date-hero.png"` - src/pages/SaveTheDatePage.jsx imports its module as `import styles from './SaveTheDatePage.module.css'` - src/pages/SaveTheDatePage.module.css contains `object-fit: cover` and the scrim `linear-gradient(` with `rgba(11, 22, 16, 0.82)` - Colors come from var(--gold-light), var(--gold), var(--cream), var(--muted); fonts from var(--font-display)/var(--font-body) - No raw hex color appears in SaveTheDatePage.module.css EXCEPT inside the scrim rgba() gradient (the documented exception) — grep `#[0-9a-f]{3,6}` returns nothing - Divider is a `<div className={styles.divider} />`, not an `<hr>` - No Framer Motion import, no CountdownTimer/GuestGreeting/BotanicalSvg/ParallaxImage usage
</acceptance_criteria>
<done>SaveTheDatePage renders hero img + scrim + bottom-anchored content block with all five content lines, styled per UI-SPEC entirely through CSS-module classes using design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SaveTheDatePage into App and build</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (current minimal shell to replace)
    - src/pages/SaveTheDatePage.jsx (the component being imported)
  </read_first>
  <action>
Replace `src/App.jsx` so it imports and renders the page. App becomes a thin wrapper (the page now owns full-viewport layout, so the old flex-centering App.module.css shell is no longer used for layout):

```jsx
import SaveTheDatePage from "./pages/SaveTheDatePage";

function App() {
  return <SaveTheDatePage />;
}

export default App;
```

Do not delete App.module.css (leave it in place; it is simply no longer imported). Do not modify main.jsx — BrowserRouter wiring from Phase 1 stays. Then run the production build to confirm the page compiles cleanly.
</action>
<verify>
<automated>grep -q "import SaveTheDatePage from './pages/SaveTheDatePage'" src/App.jsx && grep -q "<SaveTheDatePage" src/App.jsx && npm run build</automated>
</verify>
<acceptance_criteria> - src/App.jsx contains `import SaveTheDatePage from './pages/SaveTheDatePage'` - src/App.jsx renders `<SaveTheDatePage />` and no longer references the old `styles.app` shell - `npm run build` exits 0 with no errors - main.jsx is unchanged (BrowserRouter still wraps App)
</acceptance_criteria>
<done>App.jsx renders SaveTheDatePage and the project builds cleanly via `npm run build`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify the static composition</name>
  <what-built>The full static save-the-date page: hero photo filling the viewport with a bottom scrim, and a bottom-anchored content block with "SAVE THE DATE" (gold, uppercase tracked), "Aaron & Rina" (large gold-light Cormorant Garamond), a gold divider, "May 30, 2027" (cream), "Oahu, Hawaii" (cream), and "Formal invitation to follow" (muted footer).</what-built>
  <action>Pause for human review. Start the dev server (`npm run dev`) and verify the rendered static composition against the UI-SPEC using the how-to-verify steps below. This task does not modify code — it gates progression until the visual result is approved.</action>
  <how-to-verify>
    1. Run `npm run dev` and open the printed localhost URL (typically http://localhost:5173).
    2. Confirm the hero photo fills the entire viewport with no white bars or scrollbar overflow.
    3. Confirm all six text elements are visible and legible over the photo: the uppercase gold label, the large gold-light couple names, the gold divider line between names and date, the cream date and location, and the muted footer at the bottom.
    4. Confirm fonts look like Cormorant Garamond (names/date) and Jost (label/location/footer) — not a system font.
    5. Confirm there are NO animations, NO countdown timer, NO "For [name]" greeting, and NO botanical/bracket decorations (those are later phases).
  </how-to-verify>
  <read_first>
    - .planning/phases/02-static-page/02-UI-SPEC.md (compare rendered result against the contract)
  </read_first>
  <acceptance_criteria>
    - User confirms hero fills viewport, all five content lines + divider are visible and legible, and no deferred features (animation/countdown/personalization/decorations) are present
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what looks wrong (e.g., names too thin → try font-weight 400; scrim too dark/light → adjust base opacity).</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (Task 2).
- SaveTheDatePage.jsx contains all five content strings and the hero image path; module CSS uses object-fit: cover and the exact scrim gradient.
- No raw hex outside the documented scrim rgba(); no UI library imports; no Framer Motion / deferred components.
- App.jsx renders SaveTheDatePage; main.jsx (BrowserRouter) unchanged.
- Human checkpoint confirms the visual composition matches the UI-SPEC.
</verification>

<success_criteria>

1. "Aaron & Rina" renders in Cormorant Garamond at gold-light (CONT-01).
2. "Save the Date" label and "May 30, 2027" are visible (CONT-02).
3. "Oahu, Hawaii" is visible (CONT-03).
4. "Formal invitation to follow" footer is visible at the bottom (CONT-04).
5. A divider line sits between the couple names and the date block (CONT-05).
6. The hero photo fills the full viewport via object-fit: cover with no white bars or overflow (HERO-01).
   </success_criteria>

<output>
After completion, create `.planning/phases/02-static-page/02-01-SUMMARY.md`
</output>
