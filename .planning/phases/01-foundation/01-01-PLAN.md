---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/index.css
  - src/main.jsx
  - src/App.jsx
  - src/App.module.css
  - src/App.css
autonomous: true
requirements: [FND-01, FND-02, FND-03]

must_haves:
  truths:
    - "Opening the site renders a page with no console errors from a missing router context"
    - "Cormorant Garamond and Jost are requested from Google Fonts and applied via font-family; no Roboto/Inter/system fallback is declared as primary"
    - "All five palette CSS custom properties (--forest, --gold, --gold-light, --cream, --muted) are defined in :root and inherited site-wide"
    - "vite build completes with exit code 0"
  artifacts:
    - path: "index.html"
      provides: "Google Fonts three-link block (2 preconnect + 1 stylesheet) and the save-the-date title"
      contains: "family=Cormorant+Garamond"
    - path: "src/index.css"
      provides: "Five design-token CSS variables in :root + global body/root reset applying the body font"
      contains: "--gold: #BF9B5A"
    - path: "src/main.jsx"
      provides: "BrowserRouter wrapping <App>, imported from react-router"
      contains: "import { BrowserRouter } from 'react-router'"
    - path: "src/App.jsx"
      provides: "Minimal placeholder shell using a CSS Module class, no scaffold demo code"
      contains: "App.module.css"
    - path: "src/App.module.css"
      provides: "Scoped layout for the app shell"
      contains: "min-height: 100svh"
  key_links:
    - from: "src/main.jsx"
      to: "react-router BrowserRouter"
      via: "import + JSX wrap around <App>"
      pattern: "from ['\"]react-router['\"]"
    - from: "index.html"
      to: "Google Fonts CDN"
      via: "link rel=stylesheet with &display=swap"
      pattern: "display=swap"
    - from: "src/index.css body"
      to: "--font-body / Jost"
      via: "font-family: var(--font-body)"
      pattern: "font-family: var\\(--font-body\\)"
---

<objective>
Wire the Phase 1 foundation: a working React Router context, the five design-token CSS
custom properties defined site-wide, and Google Fonts (Cormorant Garamond + Jost) loaded
and applied with no system/Roboto/Inter fallback. This replaces the leftover Vite scaffold
(wrong CSS variables, Roboto font stack, dark-mode block, demo App component) with a clean
canvas that all later phases build on.

Purpose: Establishes the design system and routing context so Phase 2+ can render
save-the-date content without re-deriving tokens, fonts, or router wiring.

Output: Updated index.html, src/index.css, src/main.jsx, src/App.jsx; new
src/App.module.css; deleted src/App.css. A minimal page that renders with no console
errors and passes `npm run build`.

Scope guard: FOUNDATION ONLY. Do NOT build save-the-date content, hero image, animations,
countdown, or guest personalization — those belong to Phases 2–5. App.jsx stays a near-empty
shell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md

<binding_constraints>
CLAUDE.md is the binding design contract. The five palette colors and font choices below
are LOCKED — copy these exact values, do not re-derive:

  --forest:     #0B1610   (deep forest background)
  --gold:       #BF9B5A   (accents, labels, botanical)
  --gold-light: #D4B57A   (couple names, guest name)
  --cream:      #EAE0CB   (body text)
  --muted:      #72685A   (secondary labels, uppercase tracking)

Fonts: Cormorant Garamond (display) + Jost (body). NEVER Inter, Roboto, or system fonts as
the primary family.

IMPORTANT divergence from CLAUDE.md's prose, confirmed in 01-RESEARCH.md by direct repo
inspection: the INSTALLED stack is React 19.2.6 + react-router-dom 7.16.0 (NOT React 18 /
v6). Both are backwards-compatible. The ONE consequential difference: import BrowserRouter
from "react-router", NOT "react-router-dom" (v7 unifies the package; the -dom import path is
deprecated and emits a console warning that would violate FND-01's "no console errors").
No new npm packages are needed.
</binding_constraints>

<current_state>
Verified current files that MUST be replaced/cleaned:
- src/index.css: full Vite scaffold — defines --text/--bg/--accent, `--sans: system-ui,
  'Segoe UI', Roboto, sans-serif`, `color-scheme: light dark`, a `@media
  (prefers-color-scheme: dark)` block, and h1/h2/code rules. REPLACE WHOLESALE.
- src/App.jsx: Vite scaffold demo (logos, counter, imports ./App.css). REPLACE with shell.
- src/App.css: Vite scaffold component styles (non-module). DELETE.
- src/main.jsx: StrictMode + createRoot, NO BrowserRouter, imports ./index.css. ADD router.
- index.html: title is "wedding-site", NO Google Fonts links. ADD fonts + title.
</current_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BrowserRouter wiring and minimal App shell (FND-01)</name>
  <read_first>
    - src/main.jsx (file being modified)
    - src/App.jsx (file being modified)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1, Pattern 4, Pitfall 1, Pitfall 3)
    - CLAUDE.md (binding contract — "Wrap <App> in <BrowserRouter> in main.jsx")
  </read_first>
  <files>src/main.jsx, src/App.jsx, src/App.module.css, src/App.css</files>
  <action>
    Wire React Router context and clear the Vite scaffold demo.

    1. Replace src/main.jsx contents exactly with:

       import { StrictMode } from 'react'
       import { createRoot } from 'react-dom/client'
       import { BrowserRouter } from 'react-router'
       import './index.css'
       import App from './App.jsx'

       createRoot(document.getElementById('root')).render(
         <StrictMode>
           <BrowserRouter>
             <App />
           </BrowserRouter>
         </StrictMode>,
       )

       CRITICAL: import BrowserRouter from 'react-router' (NOT 'react-router-dom').
       The -dom path emits a deprecation warning that counts as a console error against FND-01.

    2. Replace src/App.jsx contents exactly with a minimal placeholder shell (NO scaffold
       logos, counter, or hero.png import):

       import styles from './App.module.css'

       function App() {
         return (
           <div className={styles.app}>
             {/* Save the Date content — Phase 2+ */}
           </div>
         )
       }

       export default App

    3. Create src/App.module.css with the scoped shell layout:

       .app {
         min-height: 100svh;
         display: flex;
         flex-direction: column;
         align-items: center;
         justify-content: center;
       }

    4. Delete src/App.css (Vite scaffold, non-module). It must no longer be imported anywhere.
       Run: rm src/App.css

    Do NOT add any save-the-date content, routes beyond the single page, or design values
    inline — App.jsx stays a near-empty shell for Phase 1.
  </action>
  <verify>
    <automated>grep -q "import { BrowserRouter } from 'react-router'" src/main.jsx && grep -q "<BrowserRouter>" src/main.jsx && grep -q "App.module.css" src/App.jsx && test ! -f src/App.css && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/main.jsx contains: import { BrowserRouter } from 'react-router'
    - src/main.jsx contains a <BrowserRouter> wrapping <App />
    - src/main.jsx does NOT contain the string react-router-dom
    - src/App.jsx contains: import styles from './App.module.css'
    - src/App.jsx does NOT contain "useState", "logo", or "count" (no scaffold residue)
    - src/App.module.css exists and contains: min-height: 100svh
    - src/App.css does NOT exist (test ! -f src/App.css)
    - npm run build exits 0
  </acceptance_criteria>
  <done>BrowserRouter (from react-router) wraps a minimal App shell; scaffold App.css is gone; build passes with no missing-import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Replace index.css with design tokens + global font/reset (FND-03 + FND-02 application)</name>
  <read_first>
    - src/index.css (file being REPLACED wholesale)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2, "Complete src/index.css replacement", Pitfall 2, Pitfall 4)
    - CLAUDE.md (Design system section — exact hex values)
  </read_first>
  <files>src/index.css</files>
  <action>
    REPLACE the entire contents of src/index.css (do NOT patch — the scaffold's --sans Roboto
    stack, color-scheme: light dark, dark-mode @media block, and h1/h2/code rules must all be
    removed). Write exactly:

    :root {
      --forest:     #0B1610;
      --gold:       #BF9B5A;
      --gold-light: #D4B57A;
      --cream:      #EAE0CB;
      --muted:      #72685A;

      --font-display: 'Cormorant Garamond', Georgia, serif;
      --font-body:    'Jost', system-ui, sans-serif;
    }

    *, *::before, *::after {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      background-color: var(--forest);
      color: var(--cream);
      font-family: var(--font-body);
      font-size: 16px;
      line-height: 1.5;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    #root {
      min-height: 100svh;
    }

    Notes:
    - The five palette tokens are the LOCKED values from CLAUDE.md — exact hex, do not alter.
    - body sets font-family: var(--font-body) so Jost is actually applied (loading the font
      CSS alone does not apply it — Pitfall 4).
    - Georgia/system-ui appear ONLY as last-resort fallbacks after the Google Font in the
      var() stack; the primary family is the requested Google Font. This does not violate the
      "no system fonts" rule, which targets primary families.
    - No color-scheme declaration, no dark-mode @media block, no Roboto anywhere.
  </action>
  <verify>
    <automated>grep -q -- "--forest:     #0B1610" src/index.css && grep -q -- "--gold: #BF9B5A" src/index.css && grep -q "font-family: var(--font-body)" src/index.css && ! grep -qi "roboto" src/index.css && ! grep -q "color-scheme" src/index.css && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css :root defines all five vars: --forest: #0B1610, --gold: #BF9B5A, --gold-light: #D4B57A, --cream: #EAE0CB, --muted: #72685A
    - src/index.css body rule contains: font-family: var(--font-body)
    - src/index.css body rule contains: background-color: var(--forest)
    - src/index.css does NOT contain (case-insensitive): Roboto
    - src/index.css does NOT contain: color-scheme
    - src/index.css does NOT contain: prefers-color-scheme
    - src/index.css does NOT contain the scaffold vars --text, --accent, or --code-bg
    - npm run build exits 0
  </acceptance_criteria>
  <done>index.css defines the five palette tokens in :root, applies Jost via body font-family on the forest background, and contains zero scaffold/Roboto/dark-mode residue. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Add Google Fonts three-link block and title to index.html (FND-02)</name>
  <read_first>
    - index.html (file being modified)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3, "Complete index.html head block", Pitfall 5)
    - CLAUDE.md (Fonts: Cormorant Garamond + Jost via Google Fonts)
  </read_first>
  <files>index.html</files>
  <action>
    Add the Google Fonts three-link block to <head> and set the save-the-date title.

    1. Change the title element from <title>wedding-site</title> to:
       <title>Save the Date — Aaron &amp; Rina</title>

    2. Immediately after the <title> line, add these three links (2 preconnect + 1 stylesheet).
       The crossorigin attribute on the gstatic preconnect is REQUIRED (Pitfall 5), and
       &display=swap is REQUIRED to avoid invisible-text flash (FOIT):

       <link rel="preconnect" href="https://fonts.googleapis.com" />
       <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
       <link
         rel="stylesheet"
         href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&family=Jost:wght@300;400;500;600&display=swap"
       />

    Weights requested: Cormorant Garamond 300–700 normal+italic (display font; italics needed
    for elegance in later phases), Jost 300–600 (body/labels). Do NOT add the <link> via a
    React component — fonts belong in index.html <head> to avoid a render-blocking waterfall.
    Leave the existing favicon and viewport meta lines intact.
  </action>
  <verify>
    <automated>grep -q "family=Cormorant+Garamond" index.html && grep -q "family=Jost" index.html && grep -q "display=swap" index.html && grep -q 'href="https://fonts.gstatic.com" crossorigin' index.html && grep -q "Save the Date" index.html && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains: family=Cormorant+Garamond
    - index.html contains: family=Jost
    - index.html contains: display=swap
    - index.html contains a preconnect to https://fonts.googleapis.com
    - index.html contains: href="https://fonts.gstatic.com" crossorigin
    - index.html <title> contains: Save the Date
    - index.html no longer contains the title: wedding-site
    - npm run build exits 0
  </acceptance_criteria>
  <done>index.html head requests Cormorant Garamond + Jost via the preconnect+stylescheet three-link pattern with display=swap, and the tab title reads "Save the Date — Aaron & Rina". Build passes.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `npm run build` exits 0 (no missing-import errors, no JSX errors).
2. `npm run dev`, open the served URL — page renders (empty forest canvas), browser console
   shows NO errors and NO "react-router-dom is deprecated" warning (FND-01).
3. In DevTools, inspect computed styles on :root / body — the five custom properties
   (--forest, --gold, --gold-light, --cream, --muted) are present and the body background is
   #0B1610 (FND-03).
4. In DevTools > Network (or Fonts), confirm Cormorant Garamond + Jost CSS/woff2 are fetched;
   body text renders in Jost, not a system font or Roboto (FND-02).

Manual font-rendering and computed-color checks are expected per
.planning/phases/01-foundation/01-VALIDATION.md (no browser test harness is installed).
</verification>

<success_criteria>
- FND-01: <App> is wrapped in <BrowserRouter> (imported from react-router); page renders with no router/console errors.
- FND-02: Cormorant Garamond + Jost are loaded via the index.html three-link block and applied via body font-family; no Roboto/Inter/system primary font.
- FND-03: All five palette tokens defined in :root in src/index.css and inherited site-wide; no inline design values; no scaffold/dark-mode residue.
- `npm run build` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
